# Timetable Constraints — Exhaustive Code Inventory

Auto-extracted 2026-07 by a 16-agent sweep (8 readers + 8 per-surface verifiers) over the builder & optimiser source. **188 constraints/objective terms** found; 185 verified against the cited code, 3 corrected (see end). Companion to the curated `TIMETABLE-CONSTRAINTS.md`; this one is the raw, per-surface enumeration with file:line citations.

> **Scope note.** The lecture grid is `09:00, 10:30, 10:50, 13:00, 14:30, 14:45, 16:00` (7 slots); labs are 5 slots (`09:00, 10:45, 13:00, 14:45, 16:30`). Beyond the 8 engine surfaces mapped below, the lecture grid is **also hardcoded in 4 client-side JS copies** (`page-timetable-workspace.js` defaults array + slot-editor fallback TSV; `page-timetable-workspace-split.js` `DEFAULT_LECTURE_SLOTS` + modal fallback TSV) — a cross-language duplication kept in sync by hand. Prayer compliance is **not enforced in code** (see the prayer entry below); it holds only by the curated grid start times. Deep file:line citations are approximate.

## Rule-duplication map (the headline)

The same rule is implemented/checked across many engine surfaces — the "rule-drift" the engine audit named as the #1 systemic disease. Each row = one logical rule and how many of the 8 engine surfaces touch it:

| Copies | Rule | Surfaces |
|---:|---|---|
| 8× | Instructor clash (double-booking) | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, rooming, shared-predicates, validation-publish-backstop |
| 7× | Online exclusion | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, rooming, validation-publish-backstop |
| 7× | Same-course sibling separation | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, shared-predicates, validation-publish-backstop |
| 6× | Student clash | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, validation-publish-backstop |
| 6× | Gap minutes (student idle) | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, shared-predicates |
| 6× | Instructor daily cap (<=3/day) | cpsat, flags-repairs, greedy, local-sa-rebalance, objective, shared-predicates |
| 6× | Locked placements | cpsat, flags-repairs, greedy, local-sa-rebalance, rooming, validation-publish-backstop |
| 6× | Critical overlap (H15 >=20 shared) | cpsat, flags-repairs, greedy, local-sa-rebalance, rooming, validation-publish-backstop |
| 5× | Reserve seats | cpsat, flags-repairs, greedy, objective, shared-predicates |
| 5× | Room exclusivity | flags-repairs, greedy, local-sa-rebalance, rooming, validation-publish-backstop |
| 4× | All-different-days (1 meeting/day) | cpsat, greedy, local-sa-rebalance, shared-predicates |
| 4× | Blocked slots | cpsat, greedy, local-sa-rebalance, validation-publish-backstop |
| 4× | Meeting count/duration (credits) | cpsat, flags-repairs, greedy, rooming |
| 3× | Room capacity (buffer) | flags-repairs, greedy, rooming |
| 3× | Room gender | flags-repairs, greedy, rooming |
| 3× | Room type (lab/lecture) | flags-repairs, greedy, rooming |
| 0× | Prayer windows | not enforced in code — a property of the curated grid start times only |
| 2× | Legal slot grid | greedy, validation-publish-backstop |
| 2× | Room department policy | greedy, rooming |

> A single-source design would show **1×** for every row. Instructor-clash and locks at **8×**, same-course at **7×**, the daily cap at **6×** are the drift hotspots — each is a place a future edit must be made in N files or silently diverge.

## Validation/publish/backstop (12)

### HARD
- **Placement lock respect (LOCK_RESPECT)** — lock_rejection rejects a candidate that collides with a locked cell on same day (case-insensitive via _same_day) AND same start_time (raw ==) AND same room (raw ==). Only fires when candidate has a non-empty room (line 140-141 early return); returns RejectionReason(code='LOCK_RESPECT') else None. validate_candidate ag…
  - `core/services/timetable_validation.py : lock_rejection 125-156 / validate_candidate 164-186` · stages: Candidate-generation / per-move filter only. Docstring (118-134) states this is telemetry + candidate-gen filtering, NO… · flag: TIMETABLE_ENFORCE_LOCKS, default true (config/settings.py:605, os.getenv(...,'true')). is_lock_enfo…
  - _Collision test is EXACT (day+start+room equality), not interval overlap. Per-move filter, not a whole-board gate. Roomless candidates never rejected. All accurate._
- **Same-board student time overlap (detect_board_conflicts overlaps)** — O(n^2) pairwise scan on a board. Two placements conflict when occupancy bitmasks intersect (a.mask & b.mask != 0, line 448). Classified critical when same_course OR shared_student_count >= HARD_OVERLAP_THRESHOLD (20) (line 460); warning when shared 1..19. Same-course forced via SAME_COURSE_SENTINEL(999) (line 457). Re…
  - `core/services/timetable_workspace.py : detect_board_conflicts overlaps block 445-474 (severity 460, 471); function spans 357-532` · stages: Per-board conflict detector. Feeds check_publish_readiness (critical count blocks publish) and compute_scenario_safety_… · flag: always on (detector). Student-overlap portion unconditional; instructor-identity portion (separate …
  - _Overlap uses BITMASK intersection over the fixed slot grid, NOT the half-open interval arithmetic in validation._overlaps (that helper serves auto_place_board). Per-board, not scenario-wide. Deduped by sorted placement-id pair (seen_pairs,…_
- **Same-board instructor clash (detect_board_conflicts instructor_clashes)** — Instructors grouped by identity; any two of one instructor's placements whose bitmasks intersect (a.mask & b.mask, line 484) are a clash. Identity = structured links keyed f'{course_key}|{section}' when links enabled and present (line 418-421), else free-text instructor name .strip().upper() (line 422-423). Deduped pe…
  - `core/services/timetable_workspace.py : detect_board_conflicts instructor block 476-496 (identity build 400-423)` · stages: Per-board detector. Counted CRITICAL (critical = critical_overlaps + len(instructor_clashes), line 524), so blocks publ… · flag: Identity resolution via is_instructor_links_enabled() -> TIMETABLE_INSTRUCTOR_LINKS_ENABLED, defaul…
  - _Per-board only — an instructor double-booked ACROSS two boards is invisible here (motivates the scenario-wide backstop). Bitmask overlap semantics. Accurate._
- **check_publish_readiness — publish blockers (whole-scenario gate)** — ready=False with blockers when ANY of: (1) no boards (3401-3403); (2) a board has zero placements (3413-3414); (3) a board has CRITICAL conflicts summary.critical>0 (3418-3421); (4) placements on institutionally blocked slots via blocked_slot_keys Q(day,start_time) (3424-3429); (5) room clashes>0 AND has_rooms (3440-3…
  - `core/services/timetable_workspace.py : check_publish_readiness 3368-3512` · stages: Publish/finalise gate — whole-scenario blocker check. NOT a per-move filter. · flag: always on (publish gate). Sub-checks inherit flags: blocked slots via scenario.blocked_slots; instr…
  - _Does NOT call the scenario-wide instructor backstop directly — cross-board instructor double-booking is caught here only insofar as detect_board_conflicts sees it per board (it does not across boards). Room clash blocks ONLY when has_rooms…_
- **Persist-time scenario-wide instructor-clash backstop** — scenario_instructor_clash_report reads the PERSISTED board (SectionPlacement) back and lists instructor double-bookings scenario-wide via list_instructor_clashes(sections_by_id, section_instructor_ids), EXCLUDING sibling pairs (both sections same course_code — _is_sibling_pair, 132-147). Returns {count, clashes, check…
  - `core/services/timetable_instructor_backstop.py : scenario_instructor_clash_report 85-129 / _is_sibling_pair 132-147 / verify_persisted_scen…` · stages: Called after persist by board CP-SAT solver (timetable_solver.py:678), load-balanced rebalancer (timetable_load_balance… · flag: Gated by is_instructor_links_enabled() -> TIMETABLE_INSTRUCTOR_LINKS_ENABLED, default true. Returns…
  - _HARD only at the V2 gate; SOFT/observability on every other writer (intentional asymmetry). Scenario-wide (not per board). Sibling exclusion load-bearing (same-course overlap forbidden separately). Never raises (guard in scenario_instructo…_
- **V2 rollback — student-outcome regression gate** — Rolls back if any objective position 0-3 regresses (after_value > before_value, line 87). before=baseline_score; after=score_before_instructor_passes or final_score (line 64-65). Check table by layout (66-80): tiered -> [0 highrisk_unresolved, 1 time_clashes, 2 t1_unresolved, 3 t2_over_tolerance]; non-tiered -> [0 tie…
  - `core/services/timetable_v2_runner.py : optimiser_student_outcome_regression 43-97` · stages: V2 guarded runner (run_v2_optimisation_guarded), mode='full' and mode='current'. Snapshot -> run -> regression -> rollb… · flag: always on within the V2 runner path.
  - _LIKE-FOR-LIKE / score_before_instructor_passes EXEMPTION: judges the optimiser's own pre-repair board, not final_score which includes mandatory cap/clash repair costs. No instructor pass => the two are identical (no-op). Positions 0-3 are …_
- **V2 rollback — operational safety regression gate** — Rolls back if any of 4 metrics increased after vs before (line 119): same_board_overlaps, same_board_instructors, same_board_rooms, and instructor_clashes_scenario (scenario-wide, catches cross-board instructor double-booking). Introducing even one instructor clash vetoes the run even if student outcomes improved.
  - `core/services/timetable_v2_runner.py : optimiser_safety_regression 100-129; wired in run_v2_optimisation_guarded 224-284` · stages: V2 guarded runner. safety_before = compute_scenario_safety_summary(pre-run); after = safety_before_instructor_passes if… · flag: always on within the V2 runner path. instructor_clashes_scenario sub-metric non-trivial only under …
  - _safety_before_instructor_passes EXEMPTION (line 236): gate compares against the pre-repair safety summary, so a warning-level cross-course overlap the cap/clash repair deliberately accepted does NOT trip rollback. Absent => no pass => fall…_

### SOFT
- **Same-board room clash (detect_board_conflicts room_clashes)** — Placements grouped by room (.strip().upper()), excluding online sessions and room in {'','UNASSIGNED'} (line 424). Any two same-room placements with intersecting bitmasks (line 506) are a room clash. Classified WARNING in the detector (warning = warning_overlaps + len(room_clashes), line 525).
  - `core/services/timetable_workspace.py : detect_board_conflicts room block 498-518, severity 523-525` · stages: Per-board detector. Feeds safety summary same_board_conflicts['rooms']. Promoted to publish BLOCKER only when the board… · flag: always on (detector)
  - _Warning severity in the raw detector but conditionally HARD at publish: blocks only when the board actually has assigned (non-empty, non-UNASSIGNED, offline) rooms. Online/UNASSIGNED excluded from grouping. Accurate._

### META
- **Blocked-slot exclusion set (blocked_slot_keys)** — Builds the {(day, start)} exclusion set from scenario.blocked_slots ({day,start} dicts) with RAW membership (no case-folding) so every placement stage excludes exactly the same cells. Single source of truth for the blocked-cell domain exclusion.
  - `core/services/timetable_validation.py : blocked_slot_keys 205-215` · stages: Consumed by all placement stages (per docstring: greedy _generate_meeting_options, canonical pattern catalog, per-board… · flag: always on (pure helper)
  - _Raw string membership — case-sensitive by design so all stages agree byte-for-byte. Not itself a rule; it is the shared domain used by the blocked-slot publish blocker. Accurate._
- **HARD_OVERLAP_THRESHOLD (H15 critical-overlap threshold)** — Constant = 20. Pairs with >= 20 shared students (or same-course) are HARD/critical (block publish, flagged critical, hard-blocked in local search / load-balanced). Pairs 1..19 are soft (penalised, not forbidden).
  - `core/services/timetable_overlap.py : HARD_OVERLAP_THRESHOLD 31` · stages: Referenced in detect_board_conflicts (workspace.py:437,460), other in-workspace >=20 checks (850,922,1200,1242), hard_s… · flag: always on (module constant)
  - _Single threshold shared by board-local and cross-board gates. SAME_COURSE_SENTINEL=999 (overlap.py:114) forces same-course pairs above threshold so same-course always reads critical. All line citations accurate._
- **V2 rollback — from-scratch (empty-snapshot) exemption** — Rollback fires only if blocking_regressions AND NOT board_snapshot.is_empty (line 246). A from-scratch build (scenario had no placements before the run, e.g. deferred-generate) cannot regress — nothing to protect — so it is never rolled back.
  - `core/services/timetable_v2_runner.py : run_v2_optimisation_guarded 246` · stages: V2 guarded runner; applies to the combined student + safety regression list (238-240). · flag: always on within the V2 runner path.
  - _On rollback, restore_scenario replays the snapshot (248), final_score reset to baseline_score (265), and (tiered) unresolved_students restored from baseline_unresolved_students (270-271) rather than misread from the tiered tuple. On intern…_

## Shared predicates (22)

### HARD
- **Instructor clash — whole-board predicate (has_)** — True iff any instructor has two windows of DIFFERENT sections whose half-open intervals overlap on the same day: a_start < b_end AND b_start < a_end. Same-section windows skipped; early-exits on first clash; break on day change (sorted). Empty/None section_instructor_ids -> False.
  - `core/services/timetable_constraints.py : has_instructor_clash : 101-125 (interval test _intervals_overlap 43-50; window grouping _instructo…` · stages: Shared predicate. Re-exported timetable_pr4_instructor.has_instructor_clash : 167-176. Whole-board form used for evalua… · flag: Predicate always-on (pure). Use gated by TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED (default False per …
  - _Confirmed. INTERVAL-aware, half-open (touching end==start not overlap), cross-section only. Loop at 114-124 matches; guards at 111-112 and 121-122 match. Re-export return at 176 confirmed._
- **Instructor clash — count (count_)** — Sum over all instructors of overlapping cross-section window pairs = Σ len(_overlapping_pair_list). 0 means clash-free. Empty/None -> 0.
  - `core/services/timetable_constraints.py : count_instructor_clashes : 128-139 (primitive _overlapping_pair_list 70-93; _overlapping_pairs = l…` · stages: Shared predicate; re-exported timetable_pr4_instructor.count_instructor_clashes : 179-183. Repair/diagnostic signal, no… · flag: always on (pure); same identity gating as has_ applies to real use.
  - _Confirmed. _overlapping_pairs (96-98) is len(_overlapping_pair_list) (70-93); count and list share the one primitive. Guard 134-135 matches. Re-export 179-183 confirmed._
- **Instructor clash — detail list (list_)** — One row per clashing pair: {instructor_id, day, sections:[a,b], windows:[[start,end],[start,end]]}. len(list_...) == count_...(...) always. Instructors iterated in sorted order. Empty/None -> [].
  - `core/services/timetable_constraints.py : list_instructor_clashes : 142-169` · stages: Shared predicate. Used by the persist-time backstop to report WHICH instructor is double-booked WHERE. No re-export in … · flag: always on (pure).
  - _Confirmed. sorted(windows_by_instr) at line 157; row shape at 161-168 matches exactly; derives from _overlapping_pair_list (158). Guard 153-154 matches._
- **Instructor clash — delta / per-move filter (move_introduces_)** — True iff a MOVED section now overlaps another of its instructor's sessions: (sec_i in moved OR sec_j in moved) AND intervals overlap. Empty moved set or empty section_instructor_ids -> False.
  - `core/services/timetable_constraints.py : move_introduces_instructor_clash : 172-206` · stages: Delta form for local/chain search — used INSTEAD of whole-board has_ so a pre-existing unrelated clash doesn't reject a… · flag: always on (pure); real use gated by the PR4/links flags as above.
  - _Confirmed. moved-set guard 188-190; the (sec_i in moved or sec_j in moved) AND _intervals_overlap test at 202-204 matches. PER-MOVE FILTER, interval-aware, cross-section, half-open._
- **Instructor daily-session cap — whole-board predicate (exceeds_)** — True if any (instructor_id, day) cell holds MORE THAN cap sessions (count > cap). Labs AND lectures count; a section taught by N instructors counts toward each. Pure per-(instructor,day) COUNT, no interval logic. Empty/None -> False.
  - `core/services/timetable_constraints.py : exceeds_instructor_daily_cap : 232-246 (counter _instructor_day_counts 215-229). Re-exported timet…` · stages: Structural candidate-generation filter in every solver stage per the pr4 docstring (a 4th same-day session never offere… · flag: Gated by TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (default False). Cap from TIMETABLE_INSTRUCTOR_DAIL…
  - _Confirmed. `any(c > cap ...)` at 244-246; counter increments per meeting per iid at 225-228. Strictly `>` so exactly cap allowed, cap+1 rejected. NOT in lexicographic tuple (flag-off byte-identical per pr4 docstring 103-108)._
- **Instructor daily-cap — overload count (count_)** — Total over-cap sessions = Σ over (instructor,day) of max(0, count - cap). 0 means every instructor-day within cap. Empty/None -> 0.
  - `core/services/timetable_constraints.py : count_instructor_daily_overloads : 249-264. Re-exported pr4 count_instructor_daily_overloads : 155…` · stages: Side-band diagnostic — feeds the evaluator's instructor_overload_count and the repair accept gate (pr4 docstring 160-16… · flag: Same TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (default False) / cap default 3 gating.
  - _Confirmed. max(0, c - cap) sum at 261-264; docstring 'not part of the lexicographic score' at 254-258. Re-export 155-164 confirmed._
- **Instructor daily-cap — delta / per-move filter (move_exceeds_)** — True iff a MOVED section now lands on an (instructor,day) cell whose TOTAL session count exceeds cap. Counts computed whole-board; only 'touched' cells (those a moved section occupies) tested with counts.get(cell,0) > cap. Empty moved/touched or empty ids -> False.
  - `core/services/timetable_constraints.py : move_exceeds_instructor_daily_cap : 267-293` · stages: Delta form for local/chain search — mirrors move_introduces_instructor_clash; a pre-existing over-cap on an untouched c… · flag: Same daily-cap flag (default False) + cap default 3.
  - _Confirmed. touched-set built 282-289; counts whole-board at 292; any(counts.get(cell,0) > cap for cell in touched) at 293. PER-MOVE FILTER; touched-cell total counts whole-board occupancy._
- **Same-course sibling overlap — board predicate (constraints.has_)** — True iff two sections of the SAME course_code overlap in time (interval). Groups each section's meetings by course_code into SameCourseMeetingWindow lists, then delegates to timetable_same_course.has_same_course_overlap per course.
  - `core/services/timetable_constraints.py : has_same_course_overlap : 318-323 (grouping _same_course_windows_by_course 304-315; imported as _w…` · stages: Shared predicate. Registrar convention: one instructor teaches all sections of a course, so overlapping siblings ARE an… · flag: always on (pure).
  - _Confirmed. any(_windows_have_overlap(windows) ...) at 320-323; grouping via make_meeting_window at 310-314. Import alias at 32-34 confirmed._
- **Same-course overlap — window-list primitive (same_course.has_)** — For all window pairs: skip if different course_code; skip if same non-empty section_key; flag overlap if meeting_gap_or_overlap(left,right) == -1 (left.start<right.end AND right.start<left.end, same day). Different days -> None (not overlap).
  - `core/services/timetable_same_course.py : has_same_course_overlap : 89-98 (gap primitive meeting_gap_or_overlap 46-59)` · stages: Underlying primitive used by constraints.has_same_course_overlap. · flag: always on (pure).
  - _Confirmed. course_code skip 92-93; `if left.section_key and left.section_key == right.section_key: continue` at 94-95; ==-1 test at 96-97. section_key exclusion needs distinct non-empty keys. Half-open._
- **Same-course overlap — delta / per-move filter (move_introduces_)** — True iff a MOVED section now overlaps a same-course sibling. Considers only the courses moved sections belong to; for each, pairs where section_key differs AND (one side moved) AND same day AND intervals overlap -> True. Empty moved -> False.
  - `core/services/timetable_constraints.py : move_introduces_same_course_overlap : 326-354` · stages: Delta form for local/chain search — a pre-existing same-course overlap elsewhere doesn't reject an unrelated improving … · flag: always on (pure).
  - _Confirmed. moved-set guard 336-338; moved_courses at 340; section_key-differ + (moved) + wa.day==wb.day + _intervals_overlap at 347-352. PER-MOVE FILTER using _intervals_overlap directly._

### SOFT
- **Same-course spread — candidate placement penalty** — Per candidate meeting vs same-course existing: any overlap gap==-1 -> +SAME_COURSE_OVERLAP_PENALTY (10000) and skip; all different-day (no same-day gap) -> missing_adjacent++ and +SAME_COURSE_DIFFERENT_DAY_PENALTY (1000); else nearest same-day gap: back-to-back (0<=gap<=15) -> free; else missing_adjacent++ and +_same_…
  - `core/services/timetable_same_course.py : same_course_candidate_penalty : 101-142 (constants 7-10; _same_day_gap_penalty 189-194)` · stages: Priced during generation/placement scoring (greedy candidate scoring). · flag: always on (pure helper); invoked by scorer.
  - _Confirmed. empty->0 at 106-107; overlap branch 123-125; different-day branch 128-131; back-to-back free 134-135; else penalty 137-138; final missing-adjacent block 140-141. Constants 7-10 correct; grace=15; tiers gap<=30->30, gap<=120->120…_
- **Same-course spread — whole-board evaluator penalty** — Per course with >=2 sections: anchor = each section's earliest meeting by _meeting_sort_key. For all anchor pairs: different day -> +1000; overlap(gap==-1) -> +10000; back-to-back(0..15) -> marks has_adjacent_pair; else +_same_day_gap_penalty(gap). If no adjacent pair for the course -> +5000. One-section courses neutr…
  - `core/services/timetable_same_course.py : same_course_section_spread_penalty : 145-186 (_meeting_sort_key 201-206)` · stages: Shared by timetable generation, post-processing, AND the student-assignment evaluator (docstring 150-156). · flag: always on (pure); invoked by evaluator.
  - _Confirmed. len<2 skip 159-160; anchors via min(..._meeting_sort_key) 166; pair branches 174-182; no-adjacent +5000 at 184-185. DAY_ORDER SUN=0..SAT=6 (line 11); unrecognised day -> 99 via DAY_ORDER.get(...,99) using .upper()[:3] (205)._

### META
- **Instructor identity normalisation (normalise_instructor)** — raw None -> None; strip(); empty after strip -> None; else casefold(). OPAQUE-STRING discipline (DoR A6): NO delimiter parsing/comma-splitting — 'Dr. Smith / Dr. Jones' -> 'dr. smith / dr. jones' as ONE id.
  - `core/services/timetable_pr4_instructor.py : normalise_instructor : 42-57` · stages: Used to build the free-text instructor schedule roll-up. · flag: always on (pure).
  - _Confirmed. None->None 52-53; strip + empty->None 54-56; casefold 57. A6 data-scan note (245 rows / 46 unique / zero delimiters) in module docstring 26-30._
- **Instructor schedule roll-up (build_instructor_schedule)** — Walk TermSectionMeeting for sections in board.scenario, exclude instructor=='', normalise instructor, skip if None; parse start_time 'HH:MM' -> minute (skip on ValueError/AttributeError); key = ((day or '').upper(), start_minute); dict normalised_instructor -> set of (day_upper, start_minute).
  - `core/services/timetable_pr4_instructor.py : build_instructor_schedule : 223-258` · stages: Per-run/transient; commit-3 emission path reads it to decide whether a candidate slot double-books an instructor (free-… · flag: always on function; use gated by PR4 clash flag.
  - _Confirmed. .exclude(instructor='') at 243; normalise+skip 248-250; split(':',1) parse with (ValueError, AttributeError) skip 251-255; key ((day or '').upper(), start_minute) at 256. EXACT-START keyed (day,start_minute) — NOT interval-aware…_
- **Section->instructor-id resolver (build_section_instructor_ids)** — {'course_key|section' -> {instructor_id,...}} from CourseInstructor filtered by program__in=scenario.programs, section=scenario.gender, instructor__is_active=True. Only ACTIVE instructors. Sections with no course assignment absent -> caller falls back to opaque free-text. Empty gender -> {}.
  - `core/services/timetable_pr4_instructor.py : build_section_instructor_ids : 186-220` · stages: Feeds the section_instructor_ids map consumed by the interval-aware clash/cap predicates when links flag on. · flag: Structured path gated by TIMETABLE_INSTRUCTOR_LINKS_ENABLED (default False, is_instructor_links_ena…
  - _Confirmed. empty-gender->{} 199-201; CourseInstructor.filter(program__in=programs, section=gender, instructor__is_active=True) 206-208; course_code (code or '').strip().upper() 209/214; only ids-nonempty written 218-219. Scenario-scoped._
- **PR4 instructor-clash flag helper** — is_instructor_clash_enabled() -> bool(getattr(settings, 'TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED', False)). Gates WHETHER the clash filter runs.
  - `core/services/timetable_pr4_instructor.py : is_instructor_clash_enabled : 60-63 (setting name const INSTRUCTOR_CLASH_FLAG_SETTING line 39)` · stages: Candidate-scoring loop emission of INSTRUCTOR_CLASH rejection code. · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED, default False per docstring (commit-8 promotion flips True).
  - _Confirmed. const line 39; getattr default False at 63. Independent of links flag (docstring 74-76: PR4 gates whether the filter runs, links gates where ids come from)._
- **Instructor links flag helper** — is_instructor_links_enabled() -> bool(getattr(settings,'TIMETABLE_INSTRUCTOR_LINKS_ENABLED',False)). Gates whether clash sources identity from structured CourseInstructor vs legacy opaque free-text string.
  - `core/services/timetable_pr4_instructor.py : is_instructor_links_enabled : 69-77 (const INSTRUCTOR_LINKS_FLAG_SETTING line 66)` · stages: Identity source selection for the clash/cap predicates. · flag: TIMETABLE_INSTRUCTOR_LINKS_ENABLED, default False (code default).
  - _Confirmed. const 66; getattr default False at 77. Code default here remains False (MEMORY notes course-level redesign later flipped ON via PR#43 at settings layer, not this helper)._
- **Instructor gap-penalty flag helper** — is_instructor_gap_penalty_enabled() -> bool(getattr(settings,'TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED',False)). When True the canonical evaluator appends a lowest-priority instructor_idle_minutes term (below every student+reserve term) and the greedy scorer prefers compacting placements. Off -> unchanged 6-element sc…
  - `core/services/timetable_pr4_instructor.py : is_instructor_gap_penalty_enabled : 83-93 (const INSTRUCTOR_GAP_PENALTY_FLAG_SETTING line 80)` · stages: Canonical evaluator (lowest-priority objective term) + greedy scorer. · flag: TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED, default False.
  - _Confirmed. const 80; getattr default False at 93; docstring 84-92 describes lowest-priority instructor_idle_minutes term + byte-parity/6-element tuple when off. The actual term lives in the evaluator, not this surface._
- **Instructor daily-cap enable + value helpers** — is_instructor_daily_cap_enabled() -> bool(getattr(settings,'TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED',False)); get_instructor_daily_cap() -> int(getattr(settings,'TIMETABLE_INSTRUCTOR_DAILY_CAP',3)).
  - `core/services/timetable_pr4_instructor.py : is_instructor_daily_cap_enabled : 100-110, get_instructor_daily_cap : 113-115 (consts lines 96-…` · stages: Structural candidate-generation filter in every solver stage + scenario-wide eval-driven repair pass. · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED default False; cap TIMETABLE_INSTRUCTOR_DAILY_CAP default 3.
  - _Confirmed. consts 96-97; enable default False at 110; cap default int 3 at 115. HARD, never in lexicographic tuple -> flag-off byte-identical (docstring 104-108)._
- **Instructor compaction flag + config helpers** — is_instructor_compaction_enabled() -> bool(getattr(settings,'TIMETABLE_INSTRUCTOR_COMPACTION_ENABLED',False)) — default OFF = strict no-op. get_instructor_compaction_config(): gap_budget=0.03, per_student_cap=75, trade_ratio=2.0, max_rounds=40, time_budget=20.0s (all env-overridable).
  - `core/services/timetable_pr4_instructor.py : is_instructor_compaction_enabled : 121-127, get_instructor_compaction_config : 130-142 (const l…` · stages: Post-build compaction pass (excess teaching days first, then physical-campus span/idle). · flag: TIMETABLE_INSTRUCTOR_COMPACTION_ENABLED, default False.
  - _Confirmed. const 118; enable default False 127; config values gap_budget 0.03 (133), per_student_cap 75 (135), trade_ratio 2.0 (137), max_rounds 40 (138), time_budget 20.0 (140) all match. Pass logic lives in timetable_instructor_compactio…_
- **Interval-overlap primitive (_intervals_overlap)** — True iff a_start < b_end AND b_start < a_end (half-open [start,end); touching end==start is NOT overlap).
  - `core/services/timetable_constraints.py : _intervals_overlap : 43-50` · stages: The single interval-overlap test underlying instructor-clash and same-course move-delta. · flag: always on (pure).
  - _Confirmed. `return a_start < b_end and b_start < a_end` at 50; docstring 44-49 states half-open, touching not overlap, deliberately matching meeting_gap_or_overlap._
- **Same-course gap primitive (meeting_gap_or_overlap)** — Different days -> None; overlap (left.start<right.end AND right.start<left.end) -> -1; else same-day idle gap = right.start-left.end if left.end<=right.start else left.start-right.end.
  - `core/services/timetable_same_course.py : meeting_gap_or_overlap : 46-59; is_back_to_back_gap 62-63 (0<=gap<=15)` · stages: Underlies has_same_course_overlap, has_back_to_back_pair, both spread penalties. · flag: always on (pure).
  - _Confirmed. different-day None 51-52; overlap -1 at 53-54; gap formula 55-59. is_back_to_back_gap `0 <= gap <= SAME_COURSE_BACK_TO_BACK_GRACE_MINUTES` (=15) at 62-63. Single source of same-day gap/overlap semantics._

## Greedy builder (31)

### HARD
- **One meeting per day per section** — Day sets enumerated via combinations(WEEKDAYS, num_meetings), so no candidate option ever has two meetings on one day.
  - `core/services/timetable_autoplace.py : _generate_meeting_options : 301, 337` · stages: greedy (candidate generation — structurally unrepresentable, not filtered) · flag: always on
  - _WEEKDAYS SUN-THU at line 145. combinations() at 301, consumed by the day loop at 337. Confirmed._
- **Blocked slots never used** — blocked_slots collected as exact (day,start) pairs; any position matching a blocked pair skipped. If neither slot-consistent nor fallback position on that day is unblocked, option discarded (valid=False).
  - `core/services/timetable_autoplace.py : _generate_meeting_options : 304-307, 352, 362-369` · stages: greedy (candidate generation filter) · flag: always on
  - __blocked_set built 304-307; slot-consistent check at 352; fallback loop 361-369; valid=False at 369. Exact-start match only — a blocked 10:30 does NOT block overlapping 10:50-12:05. scenario.blocked_slots read at 1461. Confirmed._
- **Meeting pattern by credit hours** — 4cr -> [75,75,100]; 3cr -> [75,75]; 2cr -> [100]; 1cr -> [75]; unknown credit falls back to the 3cr pattern. Mixed-duration patterns generate all unique permutations so the 100-min block can land on any weekly day.
  - `core/services/timetable_autoplace.py : MEETING_PATTERNS/get_meeting_pattern : 149-154, 195; permutations 318-328; consumed 1451` · stages: greedy (candidate generation) · flag: always on
  - _MEETING_PATTERNS 149-154; get_meeting_pattern return at 195 (fallback to MEETING_PATTERNS[3]); runtime perms 318-328; get_meeting_pattern(credit_hours) called at 1451. MEETING_PATTERN_VARIANTS 160-165 + get_meeting_pattern_variants 168-178…_
- **Lecture-grid vs lab-grid slot segregation** — Meetings with duration > 75 min draw positions from the lab grid (DEFAULT_LAB_SLOTS: 09:00,10:45,13:00,14:45,16:30, 100 min each); 75-min meetings from the lecture grid (DEFAULT_SLOTS: 09:00,10:30,10:50,13:00,14:30,14:45,16:00). Scenario slot_config/lab_slot_config override defaults.
  - `core/services/timetable_autoplace.py : _generate_meeting_options : 334 (lab_positions if d>75 else lecture_positions); grids 204-225; scena…` · stages: greedy (candidate generation) · flag: always on
  - _`d > 75` duration split; DEFAULT_SLOTS 204-223 (7 lecture slots incl. 14:45-16:00), DEFAULT_LAB_SLOTS 226-232 (5 labs); scenario.slot_config/lab_slot_config override defaults. Grids interleave in time (10:30-11:45 vs 10:45-12:25, and 14:30/14:45), motivating interval-overlap checks elsewhere. Confirmed._
- **Instructor clash (cross-course double-booking)** — Pre-score per-candidate filter: option rejected (never scored) if any meeting interval-overlaps (half-open: bs < m_end AND m_start < be) on the same day with another section taught by any of this section's instructors, per instructor_schedule_full. Recorded INSTRUCTOR_CLASH when tracing on.
  - `core/services/timetable_autoplace.py : auto_place_board : 1591-1627 (filter); occupancy build 1297-1350; within-run commit 2051-2069` · stages: greedy (per-move filter; a section with no surviving option is skipped) · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED, default True (config/settings.py:395-397)
  - _Filter 1591-1627 (interval-overlap test at 1612); occupancy seeded 1297-1350; within-run commit gated on instructor_clash_on (not cap flag) at 2051-2069. SCENARIO-WIDE for pre-existing meeting rows + within-run same-board placements. Ident…_
- **Instructor daily session cap (<=3/day)** — Pre-score per-candidate filter: option rejected if for any instructor and any day, existing same-day sessions (from instructor_schedule_full, excluding this section) + this option's own meetings that day exceed the cap. Lectures AND labs count. Recorded INSTRUCTOR_DAILY_CAP.
  - `core/services/timetable_autoplace.py : auto_place_board : 1637-1672; gating 1295-1296` · stages: greedy (per-move filter) · flag: daily_cap_on = TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED AND TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED — …
  - _Filter 1637-1672; daily_cap_on at 1295, daily_cap at 1296. TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED default 'true' 441-443, cap int default 3 at 444. Cap coupled to clash flag (shares occupancy structures). Cap wins vs students — no survivin…_
- **Same-course section overlap (sibling sections never share a slot)** — Pre-score per-candidate filter: option rejected if any meeting interval-overlaps (same day) any already-placed meeting of the SAME course code, via first_overlapping_same_course_window. Recorded SAME_COURSE_INSTRUCTOR_CLASH.
  - `core/services/timetable_autoplace.py : auto_place_board : 1684-1690; helper _same_course_overlap_context 472-491; predicate timetable_same_…` · stages: greedy (per-move filter) · flag: always on — no flag gate on this filter
  - _Filter 1684-1690 has no flag guard (unconditional). _same_course_overlap_context 472-491; meeting_gap_or_overlap returns -1 for overlap at 53-54. PER-BOARD scope: placed_schedule is seeded only from this board's lock preload (1379) and wit…_
- **Lock respect (registrar placement locks)** — Locked SectionPlacement rows preloaded into every occupancy structure (placed_masks, placed_schedule, all_placed_masks, room_tracker, slot_density, day_density, instr_placed); the round-robin loop structurally skips locked sections because already=count() includes them, so to_place excludes them — locked cells never r…
  - `core/services/timetable_autoplace.py : auto_place_board : 1044 (gate), 1369-1440 (preload), 1452-1458 (structural skip)` · stages: greedy (structural skip + occupancy seeding — NOT a per-candidate rejection) · flag: TIMETABLE_ENFORCE_LOCKS, default True (settings.py:605); latent until rows are locked
  - _lock_rule_on at 1044; preload loop 1369-1440 (instr_placed seeded 1380-1385, density 1387-1388, room 1389-1397); already=count() at 1452-1456, to_place at 1458. One LOCK_RESPECT row per locked cell (1398-1407). Priority PR1 locks -> PR3 wa…_
- **Warm-start baseline retention** — If a caller-supplied baseline slot matches a generated option AND that option survived every hard filter into the scored list, it is chosen as best_option regardless of score — the baseline overrides the objective but never the hard filters.
  - `core/services/timetable_autoplace.py : auto_place_board : 1822-1834; enable check 995` · stages: greedy (post-scoring override, per section) · flag: TIMETABLE_PR3_WARM_START_ENABLED, default True (settings.py:377-379); also requires baseline_placem…
  - _Retention 1822-1834 (feasible check against pr3_scored_options at 1828, swap at 1830-1831); warm_start_on = is_warm_start_enabled() at 995. Baseline days uppercased in _normalise_baseline_map (.upper() at line 871, within 868-874) or match…_
- **Room type rule: labs only for 4-credit courses** — rtype = 'lab' iff credit_hours == 4 (is_lab_course) AND meeting_requires_lab_room(duration) i.e. duration >= 80 min (unified; legacy path: duration > 80). 2-credit 100-min meetings are long LECTURES. rtype drives scoring feasibility AND actual room assignment pool.
  - `core/services/timetable_autoplace.py : auto_place_board : 1580 (cr==4 gate), 1699-1707 (scoring), 1904-1912 (assignment); predicate timetab…` · stages: greedy scoring + greedy room assignment · flag: TIMETABLE_LAB_HEURISTIC_UNIFIED default True (settings.py:523) toggles >=80 (unified) vs >80 (legac…
  - _Scoring rtype 1700-1707, assignment rtype 1905-1912, both gate on is_lab_course. Predicate returns duration >= 80 (line 102); TIMETABLE_LAB_HEURISTIC_UNIFIED default 'true' at 523. Room-assignment failure does NOT block placement — meeting…_
- **Room capacity with enrolment buffer** — section_cap = ceil(total_demand / planned_sections) x TIMETABLE_CAPACITY_BUFFER (default 1.1); a lecture room must have capacity >= section_cap. Lab meetings pass min_capacity 0 to best-fit. Rooms picked smallest-sufficient-first (pool pre-sorted capacity ASC).
  - `core/services/timetable_autoplace.py : auto_place_board : 1570-1577 (cap), 1943-1958 (best-fit); buffer timetable_rooming.py : get_capacity…` · stages: greedy room assignment (and softly in scoring via +100 feasibility penalty) · flag: TIMETABLE_CAPACITY_BUFFER env, default 1.1 (settings.py:344); capacity rule itself always on
  - _raw_cap 1572-1576, section_cap = int(raw_cap * capacity_buffer) at 1577; best-fit 1943-1958 (room_cap=0 for lab at 1947); get_capacity_buffer 50-63 (fallback 1.1); assign_best_fit 465-497 (capacity>= at 492, pool sorted ASC). ASYMMETRY con…_
- **Room occupancy (no double-booked rooms), scenario-wide, interval-aware** — A room booked in any interval overlapping [start,end) on the same day is excluded from assignment (assign_best_fit/is_feasible with end= use _overlapping_rooms: s < end AND e > start). Tracker pre-seeded with every non-online placement from ALL OTHER boards in the scenario, plus this board's locked rows, plus within-r…
  - `core/services/timetable_autoplace.py : auto_place_board : 1066-1088 (cross-board seed), 1390-1397 (lock seed), 1914-1958 (assignment); trac…` · stages: greedy room assignment · flag: always on when the board's programmes resolve rooms (room_tracker non-None)
  - _Cross-board seed 1067-1088 (online excluded 1076-1079); lock seed 1389-1397; assignment 1914-1958; _overlapping_rooms 428-430 (s<end AND e>start), mark_used 432-441, assign_best_fit 465-497. Interval-aware at assignment (end passed), exact…_
- **Room gender segregation** — board_gender derived from the board's linked students ('M'/'F' if homogeneous, '' if empty/mixed -> no filter); candidate rooms must have (room.section or '').upper() == board_gender. Applied in the pool filter and the preferred-room direct check.
  - `core/services/timetable_autoplace.py : auto_place_board : 1064, 1710, 1933-1936, 1952; timetable_rooming.py : get_board_gender : 229-249` · stages: greedy scoring (soft +100) + greedy room assignment (hard for the room) · flag: always on (degrades to no-filter when board gender is indeterminate)
  - _board_gender = get_board_gender(board_id) at 1064; passed to is_feasible at 1710; preferred-room check 1933-1936; best-fit gender arg at 1952/1953. get_board_gender 229-249 returns '' for empty/mixed. Constrains ROOM only, never the time s…_
- **Room programme (department) restriction** — The room pool for a board contains only rooms whose comma-separated Room.department intersects the board's programme code(s); boards with no programmes get no tracker and skip all rooming.
  - `core/services/timetable_autoplace.py : auto_place_board : 1057-1059; timetable_rooming.py : get_programme_rooms : 190-215` · stages: greedy room assignment (pool construction) · flag: always on
  - _programmes split 1057, room_list = get_programme_rooms(programmes) if programmes else [] at 1058, room_tracker None when empty at 1059. get_programme_rooms intersects department sets 190-215, returns capacity-ASC (order_by('capacity') at 1…_
- **Online sections occupy no room and no gap** — Online courses (OnlineCourseLookup per board) skip the room feasibility penalty, skip room assignment (room stays ''), are excluded from other-board room seeding, are excluded from the student-gap interval list when placed, and get the late-slot preference at position 4.
  - `core/services/timetable_autoplace.py : auto_place_board : 1060-1061, 1076-1080, 1478, 1901-1902; _score_option : 706-709, 761-763` · stages: greedy scoring + room assignment · flag: always on
  - _online_codes 1060-1061; other-board exclusion 1076-1079; is_online per-course 1478; assignment skip 1901-1902; gap exclusion via `if not is_online` at 706 (meetings added 708-709 only when on-campus); late-slot 761-763. Online-ness resolve…_
- **Unplaceable section is skipped, never force-placed** — If every candidate is removed by the hard filters (clash, cap, same-course) or generation produced none, best_option is None and the section is counted in total_skipped with a typed room/feasibility failure record — the builder never places a violating option as a fallback.
  - `core/services/timetable_autoplace.py : auto_place_board : 1836-1877 (None branch), 1463-1465 (no-options course)` · stages: greedy (whole-section gate) · flag: always on
  - _None branch 1836-1877 (total_skipped += 1 at 1837, refinement chain 1849-1866); no-options course path 1463-1465 (total_skipped += to_place). Mechanism realising 'cap wins vs students'. Refinement chain runs only when TIMETABLE_PR2_ROOM_OR…_

### OBJECTIVE_POSITION
- **Objective position 0: hard_conflict (reserved, always 0)** — First lexicographic element hard-coded 0 (reserved). Cross-course student conflict is NOT hard in the greedy builder — priced softly at position 2.
  - `core/services/timetable_autoplace.py : _score_option : 648, 777; final tuple 1792-1804` · stages: greedy scoring (lexicographic 6-tuple, lower-is-better, strict < at 1807) · flag: always on
  - _Off-by-one only; the rule is accurate. Strict < at 1807 means ties won by the FIRST-generated option, making candidate order (day-spacing sort, slot-index order) a real tie-break._
- **Objective position 1: same-course overlap count + same-day preference** — raw_score[1] = (+1 per bitmask overlap with an already-placed same-course section, across all groups) + (+1000 per already-placed same-course day NOT shared with this candidate — soft push for siblings onto the same day).
  - `core/services/timetable_autoplace.py : _score_option : 658-685` · stages: greedy scoring, tuple slot 2 of 6 · flag: always on
  - _same_course_overlap init 662; +1 overlap loop 663-666; +1000 different-day loop 672-685 (constant inline at 685). The +1 component is dead in practice because the always-on hard same-course filter removes overlapping options before scoring…_
- **Objective position 2: cross-course shared-student overlap (semi-hard) + same-co…** — Tuple slot 3 = student_overlap_penalty + same_course_adjacency_penalty. student_overlap_penalty = sum of shared-student counts over each already-placed different-course section whose bitmask overlaps. adjacency = same_course_candidate_penalty: 10000/overlap, 1000/different-day, graded same-day gap (<=30->30, <=120->12…
  - `core/services/timetable_autoplace.py : _score_option : 641-656 (overlap), reorder+sum 1792-1795; adjacency call 1763-1767; constants/logic …` · stages: greedy scoring, tuple slot 3 of 6 · flag: always on
  - _Overlap accumulation 650-656; reordered into slot index 2 as raw_score[5] + same_course_adjacency_penalty at 1795. same_course_candidate_penalty 101-142; constants SAME_COURSE_OVERLAP_PENALTY=10000 (line10), DIFFERENT_DAY=1000 (line9), MIS…_
- **Objective position 4: online-late / morning-gradient slot preference** — Online course: +(10 - slot_idx) per meeting (earlier -> bigger penalty). On-campus: per-meeting penalty by START TIME via _SLOT_PENALTY_BY_START {09:00->0,10:30/10:45/10:50->1,13:00->2,14:30/14:45->4,16:00/16:30->8,unknown->8} + slot_density[start] x 2.
  - `core/services/timetable_autoplace.py : _score_option : 749-768 (variable misleadingly named instructor_spread)` · stages: greedy scoring, tuple slot 5 of 6 · flag: always on
  - __SLOT_PENALTY_BY_START 749-759; online branch +(10 - slot_idx) at 761-763; on-campus branch 764-768 (+ slot_density x2 at 768). Keyed by start-time string (comment 742-748 explains lab-grid mis-pricing avoidance). Online-ness per BOARD via…_
- **Objective position 5: time consistency across days** — time_variance = number of distinct slot indices used minus one; 0 means every meeting at the same time of day.
  - `core/services/timetable_autoplace.py : _score_option : 770-774; tuple slot 6 at 1803` · stages: greedy scoring, tuple slot 6 of 6 (also counted x3 in slot 4 — double-priced) · flag: always on
  - _slot_indices/time_variance 773-774; returned as raw_score[4], placed at final tuple slot 6 (index 5) at 1803, and reused x3 as time_var_penalty in slot 4 (1740). Option generation reuses first meeting's slot_idx, falling back to first-avai…_

### SOFT
- **Objective position 3 (blended soft bucket): student idle gap** — Idle minutes between consecutive same-day intervals, computed ONLY against placed courses sharing >=1 student (overlap-matrix filter); the online course being placed is excluded; a day mixing morning+afternoon (split at 13:00) doubles any idle stretch >=60 min. Weighted by gap_weight = max(1, min(strategy_gap_multipli…
  - `core/services/timetable_autoplace.py : _score_option : 687-731 (gap); weight 1543-1544, applied 1796` · stages: greedy scoring, folded into tuple slot 4 of 6 · flag: always on; strategy sets multiplier ceiling (compact/optimal/hybrid/adaptive=10, balanced/load_bala…
  - _Gap block 687-731 (overlap-matrix filter 698-703, online exclusion via `if not is_online` at 706, midday-cross double at 728-729); gap_weight computed 1543-1544, applied at 1796 (raw_score[2]*gap_weight). STRATEGIES 394-438 with gap_multip…_
- **Objective position 3 (blended soft bucket): room-availability penalty** — +100 per meeting for which room_tracker.is_feasible(day,start,room_cap,rtype,board_gender) fails ('heavy penalty but not a hard reject'). room_cap = 0 for lab meetings (labs never capacity-filtered), else section_cap. Online sections skip this entirely.
  - `core/services/timetable_autoplace.py : auto_place_board : 1693-1712; added into tuple at 1799` · stages: greedy scoring, tuple slot 4 of 6 · flag: always on when the board resolves rooms (room_tracker non-None); no dedicated flag
  - _Room-penalty block 1693-1712 (online skip via `not is_online` at 1694, room_cap=0 for lab at 1708, +=100 at 1712); folded into slot 4 via + room_penalty at 1799. NUANCE confirmed: scoring is_feasible passes NO end (1709-1711) so it is EXAC…_
- **Objective position 3 (blended soft bucket): slot + day density load-balancing** — density_penalty += slot_density[start] x 3 + day_density[day] x 5 per meeting. slot_density counts sections placed at each start time; day_density per weekday — the x5 day term forces MON/WED to win when SUN/TUE/THU are crowded.
  - `core/services/timetable_autoplace.py : auto_place_board : 1747-1757; counters updated 2071-2072 and lock-preload 1387-1388` · stages: greedy scoring, tuple slot 4 of 6 · flag: always on
  - _density loop 1747-1757 (x3 at 1749, x5 at 1757); counters incremented at 2071-2072 and seeded in lock preload 1387-1388. slot_density ALSO added x2 inside position-4 instructor_spread for non-online (line 768) — same signal priced at two l…_
- **Objective position 3 (blended soft bucket): time-variance x3 and morning-strate…** — time_var_penalty = (distinct slot indices - 1) x 3. slot_penalty (only when strategy slot_preference > 0, i.e. morning=50): each meeting at slot_idx >= 2 adds slot_pref x (slot_idx - 1).
  - `core/services/timetable_autoplace.py : auto_place_board : 1731-1740; added 1797-1798` · stages: greedy scoring, tuple slot 4 of 6 · flag: always on for the x3 term; slot_penalty active only under strategy='morning' (slot_preference=50; a…
  - _slot_penalty 1731-1737 (guard slot_pref>0 at 1732, slot_idx>=2 at 1736); time_var_penalty = raw_score[4]*3 at 1740; both added at 1797-1798. morning slot_preference=50 (STRATEGIES line 405); default 'compact' => slot_penalty 0. Confirmed._
- **Objective position 3 (blended soft bucket): instructor idle-gap delta** — instructor_gap_penalty = sum over instructors of gap(existing_day + option) - gap(existing_day) per touched day — only idle minutes this placement ADDS — then multiplied by the SAME gap_weight as the student gap.
  - `core/services/timetable_autoplace.py : auto_place_board : 1784-1790, applied 1800-1801; delta helper _option_instructor_gap_delta 512-545; …` · stages: greedy scoring, tuple slot 4 of 6 · flag: gap_penalty_on = TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED AND TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLE…
  - _instructor_gap_penalty computed 1784-1790 (gap_penalty_on guard 1785), applied at 1801 (+ instructor_gap_penalty * gap_weight); delta helper 512-545; instr_placed seeded in lock preload 1380-1385 and within-run 2045-2050. gap_penalty_on = …_
- **Tie-break: non-consecutive-day preference via generation order** — Day combinations sorted by +1 per consecutive-day pair before generation; because best-score comparison is strict <, the first-generated option wins exact ties — giving SUN+TUE+THU a marginal edge over MON+TUE+WED that any real scored signal overrides.
  - `core/services/timetable_autoplace.py : _generate_meeting_options : 283-301; tie semantics from strict < at 1807` · stages: greedy (candidate ordering only — never in the score tuple) · flag: always on
  - __day_spacing_score 283-299 (+1 per consecutive pair at 297-298); sorted day_combos at 301; strict < at 1807. Docstring 285-293 documents the deliberate weakening from +10 to +1. Confirmed._

### META
- **Prayer compliance (grid-level, NOT enforced in code)** — No lecture slot starts 11:30-12:59, no lab slot 11:10-12:59 — a property of the fixed slot grids only. There is no grid validator and no per-meeting prayer filter anywhere, so nothing enforces this.
  - `core/services/timetable_autoplace.py : module docstring 37-40 and comment 309-312` · stages: none at greedy runtime — validated at grid definition · flag: always on (structural)
  - _Module docstring (~37-39) and the option-enumerator comment (~317) confirm no per-candidate prayer filter. A custom slot_config violating the window is caught by nothing. Confirmed._
- **Placement order policy** — Budgets loaded by descending total_demand; cross-term courses adopted onto the board holding the most students needing them (only if not already placed elsewhere); sections placed round-robin by section index; within each round courses sorted by credit_hours DESC then student count DESC.
  - `core/services/timetable_autoplace.py : auto_place_board : 1093-1098 (budgets), 1100-1161 (cross-term adoption), 1505 (rounds), 1520-1523 (r…` · stages: greedy (iteration order — shapes outcomes because earlier sections claim slots first) · flag: always on
  - _Cross-term adoption 1100-1161 (already_placed check 1129-1134, best_board check 1137-1153); round loop `for sec_round in range(...)` at 1505; sort key (-credit_hours, -len(students)) at 1520-1523. Not a constraint per se; determines which …_
- **Within-run occupancy commit contract** — After persisting each meeting, the builder updates placed_masks/placed_schedule/all_placed_masks/slot_density/day_density (always), instr_placed (gap_penalty_on), and instructor_schedule_full (instructor_clash_on — deliberately NOT gated on the cap flag).
  - `core/services/timetable_autoplace.py : auto_place_board : 2042-2072; instructor fan-out via _apply_course_instructor at 2086` · stages: greedy (state maintenance between placements) · flag: see per-structure gates above
  - _placed_masks/placed_schedule 2043-2044; instr_placed under gap_penalty_on 2045-2050; instructor_schedule_full under instructor_clash_on 2051-2069 (comment 2052-2062 explains the NOT-cap-flag gating for from-scratch rebuilds); all_placed_ma…_
- **Observability-only flags (no scheduling effect)** — Decision-trace capture (chosen slot + <=3 alternatives with typed rejection codes), stage telemetry (greedy ms + attempts), typed room-failure refinement, and the perturbation metric are all schema-stable and change no placement decision.
  - `core/services/timetable_autoplace.py : 1254 (trace gate), 1498-1503 + 2189-2193 (telemetry), 878-945 (_build_pr3_alternatives), 2208-2255 (…` · stages: greedy (observation only) · flag: TIMETABLE_PR3_DECISION_TRACE_ENABLED default True (settings.py:363-365); TIMETABLE_PR6_STAGE_TELEME…
  - _trace_enabled at 1254; telemetry setup 1498-1503, stop 2189-2193; _build_pr3_alternatives 878-945; return payload 2208-2255. Rejection codes STUDENT_CONFLICT / ROOM_OCCUPIED (classifier 786-837) + INSTRUCTOR_CLASH / INSTRUCTOR_DAILY_CAP / …_

## CP-SAT solver+polisher (31)

### HARD
- **Exactly-one option per meeting** — model.add_exactly_one(m_assign) per (section,meeting) over the non-blocked slot-option domain (slots_75 if duration<=75 else slots_lab). Empty domain => return {status:'infeasible'} instead of building an unsatisfiable exactly-one.
  - `core/services/timetable_solver.py : solve_board : 284 (also solve_board_with_hints : 821)` · stages: cpsat board solver · flag: always on
  - _Confirmed line 284 and 821. Infeasible short-circuit at 272-275 / 807-814. Whole-board gate._
- **All-different days per section** — For sections with >1 meeting, every option pair with opt_a[0]==opt_b[0] (same day_idx): add(assign[i][m_a][oa]+assign[i][m_b][ob] <= 1).
  - `core/services/timetable_solver.py : solve_board : 290-301 (also solve_board_with_hints : 826-834)` · stages: cpsat board solver · flag: always on
  - _Confirmed; day-index equality only (opt[0]). Constraint rows at 300-301 / 833-834._
- **Same-course sections no time overlap** — For same-course sections, every meeting/option pair with opt_a[5] & opt_b[5] intersecting: add(...<=1).
  - `core/services/timetable_solver.py : solve_board : 319-336 (also solve_board_with_hints : 849-863)` · stages: cpsat board solver · flag: always on
  - _Confirmed. Board loop tests ONLY the mask AND (line 335, no explicit day-equality gate) — day is baked into opt[5] via _time_mask so masks on different days never intersect; genuine interval overlap._
- **Blocked-slot domain exclusion** — Slots with (day,start) in blocked_slot_keys(scenario.blocked_slots) are skipped building slots_75/slots_lab; no assign var created; slot_idx keeps original enumerate index.
  - `core/services/timetable_solver.py : solve_board : 204-220 (also solve_board_with_hints : 741-756); blocked_slot_keys in core/services/timet…` · stages: cpsat board solver · flag: always on (data-driven by scenario.blocked_slots)
  - _Confirmed. blocked_slot_keys is exactly at validation.py:205-215, RAW (day,start) membership, no case-folding, documented single source of truth._
- **Locked same-course sibling separation (per-board)** — For each locked fixed meeting, a same-course regenerated section option with opt_a[0]==fm['day_idx'] and opt_a[5] & fm['mask'] is forced off: add(assign[i][m_a][oa]==0).
  - `core/services/timetable_solver.py : _add_locked_reservations : 170-172 (fed by _board_fixed_locked_meetings:115-144; wired at solve_board:3…` · stages: cpsat board solver · flag: TIMETABLE_ENFORCE_LOCKS — settings.py:605 default 'true' (env-overridable); is_lock_enforcement_ena…
  - _Confirmed all citations. Per-board only this board's locked SectionPlacements. fm['mask'] via _time_mask, week-level, consistent with opt[5]._
- **[POLISHER] Exactly-one option per meeting** — model.add_exactly_one(m_assign) per (section,meeting) over non-blocked domain for the meeting's duration (durations = end_min-start_min from current meetings). Empty domain => log warning + return None (abort polish).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 239 (durations 211-214)` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed line 239; empty-domain abort at 229-230._
- **[POLISHER] All-different days per section** — For movable sections with >1 meeting, option pairs on same day_idx forbidden: add(...<=1).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 244-255` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed. Only sections_to_polish get vars._
- **[POLISHER] Same-course no-overlap (movable-movable)** — For movable same-course sections, option pairs with opt_a[0]==opt_b[0] and opt_a[5] & opt_b[5] forbidden: add(...<=1).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 257-276` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed line 275 tests both day-equality AND mask AND (both operands opt[5])._
- **[POLISHER] Movable-vs-fixed same-course separation** — A movable section must not overlap a same-course fixed/locked section: for each fixed_meeting, movable options with opt_a[0]==fixed_meeting.day and opt_a[6]<fixed_meeting.end_min and fixed_meeting.start_min<opt_a[7] forced: add(assign==0).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 278-302` · stages: cpsat cross-board polisher · flag: always on (active only when fixed_sections exist — locked, or non-hotspot in hotspot mode)
  - _Confirmed explicit (day,start_min,end_min) interval via opt[6]/opt[7] at 297-302, NOT mask AND (avoids day-independent fixed-mask mismatch)._
- **[POLISHER] Instructor daily-session cap** — Per (instructor,day): sum of movable option vars <= cap - fixed_baseline, cap=get_instructor_daily_cap() (default 3); if remaining<=0 all movable vars forced to 0. Counts lectures AND labs.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 304-338` · stages: cpsat cross-board polisher · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED — settings.py:441 default 'true'; is_instructor_daily_cap_en…
  - _Confirmed all citations. Fixed sessions subtract from bound. Genuine sum<=cap per instructor-day; NOT in any lex tuple._
- **[POLISHER] Instructor clash (interval overlap)** — Per instructor, movable option pairs on different sections, same day, interval overlap (sec_a!=sec_b and day_a==day_b and s_a<e_b and s_b<e_a) mutually exclusive: add(var_a+var_b<=1). Movable option overlapping instructor's fixed/locked session forced var_a==0.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 340-384` · stages: cpsat cross-board polisher · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED — settings.py:395 default 'true'; is_instructor_clash_enable…
  - _Confirmed explicit (day_idx,start_min,end_min) interval via opt[6]/opt[7], NOT mask AND. Same-section option pairs skipped._
- **[POLISHER] Blocked-slot domain exclusion** — Options with (day,start) in blocked_slot_keys excluded building slots_75/slots_lab; slot_idx keeps original enumerate index; no var for blocked cell.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 182-202` · stages: cpsat cross-board polisher · flag: always on (data-driven)
  - _Confirmed. Polisher option tuples are 8-wide carrying end_min (opt[7]); board solver tuples 7-wide (end recomputed via _to_min(opt[4]))._
- **[POLISHER] Locked / non-hotspot sections held fixed** — Sections in locked_section_ids get NO decision vars (fixed_sections); re-added unchanged (all_sections = improved + fixed). In hotspot_only mode only hotspot_courses[:10] + overlap partners with shared>=3 become movable; rest fixed.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 143-166 (hotspot 144-162; fixed re-add 524-525)` · stages: cpsat cross-board polisher · flag: locked_section_ids param (lock enforcement upstream); hotspot_only param
  - _Confirmed. hotspot_courses[:10] at 145; count>=3 at 149/151; all_sections at 525. Fixed sections contribute constants but never move._
- **[POLISHER] Post-solve hard-reject: same-course interval overlap** — Before accept gate, has_same_course_overlap(polished_sections_by_id) checked; any two same-course sections overlapping in time (INTERVAL, not identical start) => reject outright (return None) regardless of score.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 611-616 (has_same_course_overlap in core/services/timetable_constr…` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed has_same_course_overlap at constraints.py:318-323; rejection at 611-616. Whole-board gate on final assembled result (movable+fixed)._

### SOFT
- **Same-course adjacency / spread SOFT penalties** — Per same-course section pair, reified 'both' priced by _same_course_option_penalty: different day=1000; same-day interval overlap=10000; back-to-back gap=0; else same_day_gap_penalty(gap). Plus per course (1-has_required_pair)*5000 if no adjacent pair achievable.
  - `core/services/timetable_solver.py : _add_same_course_adjacency_penalties : 70-112 (constants from core/services/timetable_same_course.py:8-…` · stages: cpsat board solver · flag: always on
  - _Confirmed constant values/line numbers: MISSING_ADJACENT_PAIR=5000 (line 8), DIFFERENT_DAY=1000 (line 9), OVERLAP=10000 (line 10). Added to scalar penalties sum, not a lex tuple._
- **Cross-course student time-overlap SOFT penalty** — For cross-course pairs with shared>0, each option pair with opt_a[5] & opt_b[5] gets reified 'both' weighted shared*5.
  - `core/services/timetable_solver.py : solve_board : 360-388 (also hints:868-892)` · stages: cpsat board solver · flag: always on
  - _Confirmed. Explicit comment 314-316 marks cross-course overlap SOFT not hard. shared via shared_student_count._
- **Locked cross-course SOFT push-off** — For a cross-course locked fixed meeting with shared>0, options on the locked day whose mask intersects: penalties.append(assign[i][m_a][oa] * (shared*5)).
  - `core/services/timetable_solver.py : _add_locked_reservations : 173-174` · stages: cpsat board solver · flag: TIMETABLE_ENFORCE_LOCKS (settings.py:605 default 'true'; getattr fallback False) — no-op when fixed…
  - _Confirmed lines 173-174. Same shared*5 weight family as movable cross-course term._
- **Real idle-gap SOFT penalty (cross-course, same day)** — Cross-course pairs, shared>0, NEITHER online; same-day option pairs compute gap; skip gap<=15; multiply by 1.5 if crossing 13:00; reified 'both' priced both*gap*w, w=min(shared,30).
  - `core/services/timetable_solver.py : solve_board : 405-456 (also hints:906-949)` · stages: cpsat board solver · flag: always on
  - _Confirmed. Online exclusion at 413-414. MIDDAY_BOUNDARY=13*60 at line 52. Models actual minute gap._
- **Online-course late-slot SOFT preference** — Per online section option, early_penalty = max(0,(num_slots-1-slot_idx))*5 added as assign*early_penalty.
  - `core/services/timetable_solver.py : solve_board : 458-468 (also hints:951-957)` · stages: cpsat board solver · flag: always on (only fires for is_online sections from OnlineCourseLookup per-board)
  - _Confirmed. num_slots=len(slot_config); is_online resolved per BOARD via OnlineCourseLookup().codes_for_board._
- **Earlier-slot SOFT preference (multi-meeting)** — For sections with >1 meeting, each option priced assign*opt[1]*2.
  - `core/services/timetable_solver.py : solve_board : 470-479 (also hints:959-965)` · stages: cpsat board solver · flag: always on
  - _Confirmed w=2, comment 'uniform (no S1 priority)'._
- **Slot-index consistency SOFT penalty (lecture meetings)** — For sections with >1 lecture meeting (duration<=75), lecture-meeting option pairs at different slot indices: reified 'both'*diff*10, diff=abs(slot_idx diff).
  - `core/services/timetable_solver.py : solve_board : 481-502 (also hints:967-987)` · stages: cpsat board solver · flag: always on
  - _Confirmed w=10; only lecture meetings compared._
- **Consecutive-day SOFT penalty (day spacing)** — For sections with >1 meeting, option pairs on consecutive days (abs(opt_a[0]-opt_b[0])==1): reified 'both'*5.
  - `core/services/timetable_solver.py : solve_board : 504-522 (also hints:989-1006)` · stages: cpsat board solver · flag: always on
  - _Confirmed w=5._
- **[POLISHER] Cross-course student-overlap SOFT penalty (movable-movable)** — For movable cross-course pairs with shared>0, option pairs with opt_a[0]==opt_b[0] and opt_a[5] & opt_b[5] add reified penalty priced shared*5. overlap from build_cross_board_overlap_matrix (scenario-wide).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 386-407 (reification 424-436)` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed. Scenario-wide overlap matrix (all courses across all boards), unlike board solver's per-board matrix._
- **[POLISHER] Cross-course overlap-with-FIXED SOFT penalty** — For a movable section vs fixed section sharing students>0, options with opt_a[0]==fixed_meeting.day and opt_a[5] & fixed_meeting.mask add single-var penalty (var_a, weight=shared*5).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 409-422` · stages: cpsat cross-board polisher · flag: always on (active only when fixed_sections exist)
  - _Confirmed line 421 AND-s opt_a[5] (week-level, day baked) with fixed_meeting.mask (day-independent) — the same inert-off-SUN mismatch the HARD movable-vs-fixed term was rewritten to avoid; day-equality gate (opt_a[0]==fixed_meeting.day) pr…_

### META
- **Objective = scalar weighted-sum minimize (board solver)** — model.minimize(sum(penalties)) — single scalar weighted sum of all SOFT terms; NO lexicographic tuple in the CP-SAT board model. Empty penalties => objective skipped, reported objective=0.
  - `core/services/timetable_solver.py : solve_board : 524-525 (also hints:1008-1009); objective read at 578 / 1072` · stages: cpsat board solver · flag: always on
  - _Confirmed. Solver params max_time_in_seconds, num_workers=8, random_seed=42 at 528-531. INFEASIBLE/timeout => empty placements at 542-543._
- **solve_board_with_hints warm-start + count-based 'improved' flag** — Greedy placements fed as add_hint for matching (day,start) option (1017-1027). improved = (len(placements) >= greedy_count); placed_vs_greedy = len(placements)-greedy_count — a COUNT comparison, NOT objective/score. No accept-gate discarding a worse-objective solution.
  - `core/services/timetable_solver.py : solve_board_with_hints : 1011-1081` · stages: cpsat board solver (hint variant) · flag: always on
  - _Confirmed. Model built identically to solve_board. improved at 1079, placed_vs_greedy at 1080; no lex/objective verification._
- **NO instructor clash / daily-cap constraint IN the board solver model** — solve_board and solve_board_with_hints have NO instructor variable and add NO instructor-clash/daily-cap constraint. Instructor safety on this surface is only a POST-PERSIST backstop: persist_solver_result calls verify_persisted_scenario(scenario_id, context='board_cpsat_solver') which records/verifies but does not al…
  - `core/services/timetable_solver.py : persist_solver_result : 678-680 (docstring 591-597)` · stages: cpsat board solver (persist path) · flag: backstop via timetable_instructor_backstop.verify_persisted_scenario
  - _Confirmed. Contrasts with polisher (models clash+cap as HARD). persist re-fans apply_primary_instructor at 676; preserves locked rows on re-solve when TIMETABLE_ENFORCE_LOCKS on at 619-620._
- **[POLISHER] Objective = scalar weighted-sum minimize (PROXY)** — model.minimize(sum(var*w for var,w in penalty_vars)) — scalar weighted overlap-penalty sum. Explicit PROXY: does NOT model full student assignment, capacity/reserve, or real gaps.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 424-436` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed. Proxy comment at 594-597. Solver params num_workers=8, random_seed=42, max_time_in_seconds at 460-463. Warm-start hints matched by (day,start_min) at 438-450._
- **[POLISHER] Accept gate — full-evaluator strict lexicographic improvement** — Polished board (improved movable + fixed) re-scored by evaluate_generated_timetable_candidate; ACCEPTED only if polished_eval.lexicographic_score < current_eval.lexicographic_score (strictly better on the REAL lex tuple); else return None. Solver status must be OPTIMAL/FEASIBLE else return None.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 477-479 (status), 598-635 (verify + gate)` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed status guard 477-479, evaluator call 598-605, strict-less gate at 618. Lex tuple imported from evaluator, not the CP-SAT model — why this surface has no OBJECTIVE_POSITION rows of its own._
- **[POLISHER] Online marker carried through** — Each rebuilt SectionState carries is_online=sec.is_online, else the polished board would be rescored as if every online session were on campus.
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 519-521` · stages: cpsat cross-board polisher · flag: always on
  - _Confirmed. Correctness contract for accept-gate re-evaluation, not a model constraint._
- **[POLISHER] Stage telemetry + decision trace (observability)** — Telemetry on: record cpsat stage ms + iterations=1 (guardrail: iterations==1 means solver.solve() reached). Trace on: emit per-changed-section DecisionTrace (code CPSAT_IMPROVED, previous/new slot).
  - `core/services/timetable_cpsat_polisher.py : polish_scenario_with_cpsat : 469-474 (telemetry), 528-591 (trace)` · stages: cpsat cross-board polisher · flag: telemetry: TIMETABLE_PR6_STAGE_TELEMETRY_ENABLED (settings.py:559 default 'true') AND stage_telemet…
  - _Confirmed. Pure observability; no effect on placement/accept. Early returns above the solve() fence leave cpsat telemetry at zero._

## Local/chain/SA/rebalance (25)

### HARD
- **High-overlap pair block (shared >= HARD_OVERLAP_THRESHOLD)** — _check_hard_constraints: for pairs of meetings from DIFFERENT courses whose masks overlap (mask AND != 0), reject if shared >= HARD_OVERLAP_THRESHOLD (20). No matrix -> shared = SAME_COURSE_SENTINEL (999) >= 20, so ANY cross-course mask overlap rejects.
  - `core/services/timetable_local_search.py:_check_hard_constraints:203-214 (threshold import 189-192); timetable_load_balanced.py:_check_const…` · stages: SA polish per-move gate; load-balanced per-move gate · flag: always on
  - _Confirmed. SA pair loop 203-214, import 189-192; load-balanced mirror 232-243. HARD_OVERLAP_THRESHOLD=20 (timetable_overlap.py:31), SAME_COURSE_SENTINEL=999 (timetable_overlap.py:114). WHOLE-BOARD per-move, mask-based (interval-via-mask). …_
- **Same-course sibling overlap reject** — _check_hard_constraints returns False if two sections of the SAME course have overlapping meeting windows (true interval overlap via timetable_same_course primitives).
  - `core/services/timetable_local_search.py:_check_hard_constraints:217-218 via _has_same_course_overlap:103-107; timetable_load_balanced.py:_c…` · stages: SA polish per-move gate; load-balanced per-move gate · flag: always on
  - _Confirmed. SA 217-218 via helper 103-107; load-balanced 245-246 via helper 69-73. WHOLE-BOARD per move, interval-aware (half-open). Accurate._
- **All-different-days per section** — _check_hard_constraints returns False if any single section has two meetings on the same day (len(days) != len(set(days))).
  - `core/services/timetable_local_search.py:_check_hard_constraints:221-224; timetable_load_balanced.py:_check_constraints:248-251` · stages: SA polish per-move gate; load-balanced per-move gate · flag: always on
  - _Confirmed. SA 221-224; load-balanced 248-251. WHOLE-BOARD per move. Accurate._
- **Instructor daily-session cap (SA / rebalance in-memory count)** — _check_hard_constraints: when cap flag on, count sessions per (instructor_id, day) over sections carrying resolved 'instructor_ids'; reject if any count > cap. Labs+lectures count; days are strings; N-instructor section counts toward each.
  - `core/services/timetable_local_search.py:_check_hard_constraints:231-244; instructor_ids resolved in optimize_board:456-461 via build_sectio…` · stages: SA polish per-move gate ONLY (load-balanced _check_constraints has NO cap check — instructor-blind) · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED, effective default true (config/settings.py:441-442); cap TI…
  - _Confirmed. Cap block 231-244; ids resolved 456-461. Load-balanced _check_constraints (216-253) genuinely has no cap check. Effective runtime default is true (settings.py:441-442 sets "true"); the pr4 helper's getattr fallback docstring say…_
- **Instructor clash (interval overlap) — SA in-memory gate** — _check_hard_constraints: when clash flag on, build per-instructor windows (day,start_min,end_min,section_idx); reject if two DIFFERENT sections of the same instructor overlap on the same day. Interval overlap (s_a < e_b and s_b < e_a), not identical start.
  - `core/services/timetable_local_search.py:_check_hard_constraints:250-271` · stages: SA polish per-move gate ONLY (load-balanced rebalancer has NO in-memory clash check) · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED, effective default true (config/settings.py:395-396)
  - _Confirmed. Clash block 250-271; interval test at 270 (sec_a != sec_b and s_a < e_b and s_b < e_a); day-change break at 268-269; ids resolved 456-461. SA feasibility is BOARD-SCOPED (loads filter(board=board) at 383) so cross-board clashes …_
- **Blocked-slot exclusion (SA relocate domain)** — valid_options excludes any (day, slot.start) in blocked_slot_keys(scenario.blocked_slots). Applied for lecture (<=75min) and lab (>75min) grids.
  - `core/services/timetable_local_search.py:optimize_board:484,503,518; timetable_load_balanced.py:rebalance_board:293,314,328` · stages: SA polish (domain construction); load-balanced rebalancer (domain construction) · flag: always on
  - _Confirmed. SA blocked_set at 484, lecture skip 503, lab skip 518; load-balanced blocked_set 293, lecture skip 314, lab skip 328. Per-move DOMAIN filter. Accurate._
- **Locked-section immobility (SA / rebalance)** — Locked sections never chosen as relocate/move target: locked_idx set; _generate_relocate_move returns None if sec_idx in locked_idx (SA); rebalance_board continues if sec_idx in locked_idx. locked_idx populated only when lock flag on.
  - `core/services/timetable_local_search.py:optimize_board:485-489, _generate_relocate_move:293-294; timetable_load_balanced.py:rebalance_board…` · stages: SA polish move-generation; load-balanced rebalancer move-generation · flag: TIMETABLE_ENFORCE_LOCKS, default true (config/settings.py:605 via is_lock_enforcement_enabled)
  - _Confirmed. SA locked_idx 485-489, burn-draw 293-294; load-balanced locked_idx 294-298, continue 354-355; TIMETABLE_ENFORCE_LOCKS assignment at settings.py:605. Persist-time lock exclusion also confirmed (optimize 787-802, rebalance 441-453…_
- **Same-course overlap delta reject (LS-v2 single-move)** — In diagnostic_driven_local_search, after apply_move_to_grid, move_introduces_same_course_overlap(sections_by_id, _moved) rejects (rollback+continue) if a MOVED section now overlaps a same-course sibling. Delta-scoped.
  - `core/services/timetable_local_search_v2.py:diagnostic_driven_local_search:285-287; delta impl timetable_constraints.py:move_introduces_same…` · stages: Local search v2 (single-move best/first-improvement) · flag: always on
  - _Confirmed. v2 reject 285-287; _moved built 279-281; delta impl 326-354 (interval overlap via _intervals_overlap, half-open). DELTA-scoped. Accurate._
- **Instructor daily-cap delta reject (LS-v2 single-move)** — move_exceeds_instructor_daily_cap(sections_by_id, section_instructor_ids, cap, _moved) rejects if a moved section lands on an (instructor,day) cell whose total count > cap. Only cells the moved section occupies are checked.
  - `core/services/timetable_local_search_v2.py:diagnostic_driven_local_search:292-300; delta impl timetable_constraints.py:move_exceeds_instruc…` · stages: Local search v2 single-move · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (effective default true) AND section_instructor_ids provided…
  - _Confirmed. v2 guarded reject 292-300 (guard 'section_instructor_ids and is_instructor_daily_cap_enabled()'); delta impl 267-293 (touched-cells only). Accurate._
- **Instructor-clash delta reject (LS-v2 single-move)** — move_introduces_instructor_clash(sections_by_id, section_instructor_ids, _moved) rejects if a moved section now overlaps (interval) another session of its instructor on the same day.
  - `core/services/timetable_local_search_v2.py:diagnostic_driven_local_search:304-310; delta impl timetable_constraints.py:move_introduces_inst…` · stages: Local search v2 single-move · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED (effective default true) AND section_instructor_ids provided
  - _Confirmed. v2 reject 304-310; delta impl 172-206. Runs before room-feasibility (313-323) and before evaluation (326). DELTA-scoped interval overlap. Accurate._
- **Room feasibility repair (LS-v2 single-move)** — When rooms_by_id and room_occupancies supplied, try_repair_rooms_locally must succeed for the moved snapshot; on False the move is rolled back and skipped.
  - `core/services/timetable_local_search_v2.py:diagnostic_driven_local_search:313-323` · stages: Local search v2 single-move (room-aware runs only) · flag: always on when room maps passed; skipped when rooms_by_id/room_occupancies are None (roomless mode)
  - _Confirmed. Room gate 313-323 (guard 'rooms_by_id is not None and room_occupancies is not None'); roomless _rollback branch at v2:143-148. Accurate._
- **Locked-section exclusion (LS-v2 / chain move generation)** — generate_all_repattern_moves and generate_swap_moves skip any sec_id in locked_section_ids; chain generation skips locked h_sid/p_sid. Locked sections never offered as move source or swap partner.
  - `core/services/timetable_local_search_v2.py:generate_all_repattern_moves:57-59, generate_swap_moves:106-113; timetable_local_search_chains.p…` · stages: Local search v2 + chain search move generation · flag: gated by caller passing locked_section_ids (populated from lock enforcement, TIMETABLE_ENFORCE_LOCK…
  - _Confirmed. repattern skip 57-59; swap skips 106-107 and 112-113; chain skips 161-162 (h_sid) and 175-176 (p_sid). 'locked = locked_section_ids or set()' defaults empty. DOMAIN filter at generation. Accurate._
- **Swap-move validity constraints (LS-v2)** — generate_swap_moves pairs only sections in the same pattern_family with >=2 sections and >=2 family patterns, requires section_a.course_code in top-10 hotspot set, forbids same-course pairs and identical-pattern pairs.
  - `core/services/timetable_local_search_v2.py:generate_swap_moves:98-131` · stages: Local search v2 phase-2 swaps · flag: always on
  - _Confirmed. hotspot_set = set(hotspot_courses[:10]) at 89; family >=2 at 99; family_pats >=2 at 102; hotspot membership 109; same-course forbid 115; same-pattern forbid 117. Structural generation constraints. Accurate._
- **Same-course overlap delta reject (chain-2)** — After applying both chain legs, move_introduces_same_course_overlap over moved sections rejects (rollback both legs) if a moved leg now overlaps a same-course sibling.
  - `core/services/timetable_local_search_chains.py:chain_local_search:360-363` · stages: Chain-2 search · flag: always on
  - _Confirmed. Reject 360-363; _moved built 352-355 = {move_a.section_id_a, move_b.section_id_a} + any section_id_b. DELTA-scoped. Accurate._
- **Instructor daily-cap delta reject (chain-2)** — move_exceeds_instructor_daily_cap over the chain's moved sections rejects if any moved leg lands on an over-cap (instructor,day) cell.
  - `core/services/timetable_local_search_chains.py:chain_local_search:367-375` · stages: Chain-2 search · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (effective default true) AND section_instructor_ids provided…
  - _Confirmed. Guarded reject 367-375. DELTA-scoped, no-op if ids absent or flag off. Accurate._
- **Instructor-clash delta reject (chain-2)** — move_introduces_instructor_clash over the chain's moved sections rejects if either leg now double-books an instructor (interval overlap).
  - `core/services/timetable_local_search_chains.py:chain_local_search:379-385` · stages: Chain-2 search · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED (effective default true) AND section_instructor_ids provided
  - _Confirmed. Guarded reject 379-385; evaluate follows directly at 388 with NO room-feasibility repair in the chain reject sequence (unlike LS-v2 single-move at 313-323) — extraction's note is accurate. Accurate._

### SOFT
- **SA private gap-cost objective (overlap-weighted idle gap minutes)** — _compute_cost sums over unordered pairs of DIFFERENT campus courses sharing students the idle-gap minutes on each shared day. w = min(shared,30)*0.5 with overlap_matrix, else 10.0. Same-course pairs skipped; online skipped; only positive gaps; midday(13:00)-straddling gap x1.5. SA's private cost, not the canonical stu…
  - `core/services/timetable_local_search.py:_compute_cost:110-178 (pair loop 147-176, weight 157-163, midday 173-174)` · stages: SA polish (optimize_board acceptance loop) · flag: always on (gated only by whether optimize_board is invoked)
  - _Confirmed. _compute_cost at 110; pair loop 147-176; w at 161 (min(shared,30)*0.5) / 163 (10.0); online skip at 134; same-course skip 155-156; MIDDAY_BOUNDARY=13*60 at line 81; x1.5 at 173-174. Accurate._
- **Same-course section-spread penalty (SA cost term)** — _compute_cost adds same_course_section_spread_penalty over per-course meeting windows as a flat addend. Load-balanced reuses the same helper in its balance score.
  - `core/services/timetable_local_search.py:_compute_cost:177 via _same_course_spread_cost:99-100; timetable_load_balanced.py:_compute_balance_…` · stages: SA polish; load-balanced rebalancer balance score · flag: always on
  - _Confirmed. SA line 177 adds _same_course_spread_cost (99-100); load-balanced line 213 adds it into imbalance+gap_total. Accurate._
- **Load-balanced daily-load balance objective (max-min imbalance + gaps)** — _compute_balance_score = imbalance + gap_total + same_course_spread. imbalance=(max_load-min_load)*100 over overlap-weighted per-day campus course counts. gap_total = overlap-weighted idle-gap minutes (w=min(shared,30), else 10; online skipped; NO midday multiplier). Daily load weight=max(1, course_overlap_load).
  - `core/services/timetable_load_balanced.py:_compute_balance_score:168-213, _daily_load:151-165` · stages: Load-balanced rebalancer · flag: always on
  - _Confirmed. _compute_balance_score 168-213; imbalance x100 at 179; gap weight 203 (min(shared,30)) / 205 (10); no x1.5 in gap loop 208-211; _daily_load 151-165, load weight max(1, course_overlap_load) at 162; online skip 160-161/187-188. Di…_

### META
- **SA acceptance gate (Metropolis on private gap-cost)** — After _check_hard_constraints passes, delta=new_cost-current_cost. delta<0 accept (track best). Else if temperature>0.01 and rng.random()<exp(-delta/temperature) accept. Else undo. best restored at end. Loop until wall-clock max_seconds.
  - `core/services/timetable_local_search.py:optimize_board:553-597 (accept 575-587)` · stages: SA polish · flag: always on
  - _Confirmed. while loop 553; accept 575-587; best captured 579-581. initial_temp=500 (348), cooling 0.9995 (349), seed 42 (350). Operates on private gap-cost. Accurate._
- **SA evaluator gate (WS-B student-score regression rollback)** — optimize_and_persist_board captures pre-SA canonical lexicographic student score (_sa_scenario_score -> evaluate_generated_timetable_candidate) + placement snapshot before persisting. After persist+reroom, recompute _after_score; if strictly worse (tuple compare), restore snapshot and reroom. Inactive when no student …
  - `core/services/timetable_local_search.py:optimize_and_persist_board:764-861 (capture 771-779, rollback 852-861); _sa_scenario_score:689-721` · stages: SA polish persist step (whole-scenario score, single-board rollback) · flag: always on when student profiles exist; tier map threaded only when TIMETABLE_TIERED_OBJECTIVE_ENABL…
  - _Confirmed in-file. Capture 771-779; rollback 852-861; _sa_scenario_score 689-721 (returns tuple(evaluation.lexicographic_score) at 721); snapshot fields _SA_SNAPSHOT_FIELDS 658-666. SCENARIO-wide score, BOARD-scoped snapshot. Tier flag set…_
- **Instructor-clash persist-time backstop (SA / rebalance)** — After persist, verify_persisted_scenario scans the whole scenario's persisted placements for instructor double-bookings (interval). Single-board callers pass verify_instructor_clashes=True; scenario-loop callers pass False and scan once after the last board.
  - `core/services/timetable_local_search.py:optimize_and_persist_board:863-868, optimize_scenario:890-899; timetable_load_balanced.py:rebalance…` · stages: SA polish persist; load-balanced rebalancer persist · flag: always on (backstop internals honour clash / links flags)
  - _Confirmed. SA persist 863-868 (context 'sa_polish'), optimize_scenario 890-899 (context 'sa_polish_scenario', verify=False at 883); load-balanced 494-497 (context 'load_balanced_rebalance'), rebalance_scenario 514-521 (verify=False at 511)…_
- **LS-v2 best/first-improvement accept gate (canonical objective)** — Surviving move scored by evaluate_generated_timetable_candidate (canonical lexicographic, tier-threaded). Kept only if lexicographic_score < best_move_score. Best-improvement <=80 sections (try all, apply best); first-improvement >80 (accept first improver). Stops on no-improvement / 180s budget / max_iterations (defa…
  - `core/services/timetable_local_search_v2.py:diagnostic_driven_local_search:257-378 (accept 336-344, threshold 183, budget 188-207)` · stages: Local search v2 · flag: always on; tier map threaded only when TIMETABLE_TIERED_OBJECTIVE_ENABLED (default true)
  - _Confirmed. Accept 336-344 (strict '<' at 336); use_first_improvement = len>80 at 183; budget time_budget_seconds=180.0 at 188, checked 200-207; max_iterations default 50 at 159. section_instructor_ids + course_tiers passed to evaluator (33…_
- **Chain-2 best-improvement accept gate (canonical objective)** — Each surviving chain scored by evaluate_generated_timetable_candidate; kept only if lexicographic_score < best_chain_score. Best chain re-applied permanently. Stops on no-improvement, max_iterations (default 10), or chain_time_limit_seconds deadline (pre-iteration + mid-scan).
  - `core/services/timetable_local_search_chains.py:chain_local_search:337-500 (accept 398-401, reapply 424, deadline 302-311,409-411)` · stages: Chain-2 search · flag: always on; tier threaded only when TIMETABLE_TIERED_OBJECTIVE_ENABLED (default true)
  - _Confirmed. Accept 398-401 (strict '<'); reapply 424; deadline construct 302-306, pre-iteration check 309-311, mid-scan 409-411; max_iterations default 10 at 267. section_instructor_ids + course_tiers passed at 393-394. Strictly-improving. …_
- **Load-balanced acceptance gate (Metropolis on balance score)** — After a random meeting relocation passes _check_constraints, delta=new_score-current_score. Accept if delta<0 OR (temperature>0.01 and rng.random()<exp(-delta/temperature)); else revert. best captured when new_score<best_score. temperature starts 300.0, x0.9995/iter, seed 42. Runs until max_seconds.
  - `core/services/timetable_load_balanced.py:rebalance_board:348-389 (accept 380-388)` · stages: Load-balanced rebalancer · flag: always on
  - _Confirmed. while loop 348; accept 380-388; temperature init 300.0 at 346, x0.9995 at 350; seed default 42 (signature 256, rng at 300). NO student-assignment evaluator rollback gate and NO in-memory instructor constraints — only balance sco…_

## Rooming (12)

### HARD
- **Room TYPE match (lab vs lecture)** — Required type via _room_type_for_credit_duration: unified ON (default) -> 'lab' iff credit_hours==4 AND meeting_requires_lab_room(duration>=80, inclusive) else 'lecture'; unified OFF (legacy) -> 'lab' iff duration>80 AND credit_hours==4. credit resolved per-placement from budget 'credit' bucket, default 3. RoomTracker…
  - `core/services/timetable_rooming.py : _room_type_for_credit_duration : 151-162 ; room_type_for_placement : 165-187 ; RoomTracker._pool : 417…` · stages: Greedy per-move pool; assign_rooms_to_board filler (line 929 classify / 933 room_cap); publish-gate backstop H11 · flag: TIMETABLE_LAB_HEURISTIC_UNIFIED default true (config/settings.py:523); type filter itself always on
  - _All citations confirmed. Legacy > vs unified >= off-by-one on 80-min meetings confirmed at lab_predicate.py:92-102. credit==4 gate is layered inside _room_type_for_credit_duration, not in the duration predicate — accurate._
- **Room CAPACITY >= buffered demand (lecture rooms only in greedy)** — Lecture rooms need capacity >= buffered = int(raw * TIMETABLE_CAPACITY_BUFFER), raw = ceil(total_demand/planned_sections) or max_per_section (default 40) when planned_sections<=0. LAB meetings force room_cap=0 (line 933) so any correctly-typed/gendered lab room passes regardless of size. assign_best_fit picks smallest…
  - `core/services/timetable_rooming.py : get_capacity_buffer : 50-63 ; _raw_section_demand : 88-92 ; _build_rooming_budget_maps (buffered=int(r…` · stages: Greedy per-move; assign_rooms_to_board filler per-move; H12 backstop applies capacity to ALL types incl. labs (no lab e… · flag: TIMETABLE_CAPACITY_BUFFER default 1.1 (config/settings.py:344); lecture capacity filter always on
  - _Citations confirmed. Minor: the buffered assignment is specifically line 113 (within the extracted 112-120 span). LAB capacity divergence real: greedy room_cap=0 vs H12 (line 331) with no lab exemption — a too-small lab room passes greedy …_
- **Room GENDER match (M/F)** — Required gender = _section_gender(TermSection.section first char if M/F) else board-level get_board_gender (single shared Student.section across all linked students, '' if empty/mixed). _pool filters pool to rooms whose section==required gender when gender in (M,F); '' disables the filter. H13 backstop: not required_g…
  - `core/services/timetable_rooming.py : _section_gender : 218-226 ; get_board_gender : 229-249 ; RoomTracker._pool : 417-421 ; assign_rooms_to…` · stages: Greedy per-move pool; filler per-move; publish-gate backstop H13 · flag: always on (no gating flag; empty gender = no constraint)
  - _All citations confirmed. Per-section label wins over board gender (line 936). Mixed/empty board -> '' -> filter disabled._
- **Room DEPARTMENT / programme policy** — A room is eligible for a board iff its comma-separated department field shares >=1 programme code with the board's programmes (case-insensitive set intersection). get_programme_rooms builds the candidate list from this rule; failing rooms never enter the tracker. H14 backstop: (board.program set) & (room.department se…
  - `core/services/timetable_rooming.py : get_programme_rooms : 190-215 (match 203-204) ; assign_rooms_to_board (rooms=get_programme_rooms) : 77…` · stages: Room-pool construction upstream of all placement (per-board); publish-gate backstop H14 · flag: always on
  - _Citations confirmed. The call site is line 773 (line 757 in the original citation is the programmes-parse line, not the get_programme_rooms call — both are accurate references but 773 is the actual call). Any single overlap qualifies the r…_
- **Room OCCUPANCY / no double-booking (overlap-aware)** — A room cannot host two overlapping meetings on the same day. RoomTracker keeps usage[(day,start)] (exact) and intervals[day]=list of (start_min,end_min,room). _overlapping_rooms returns rooms with s<end AND e>start. With end= passed, assign_best_fit/is_feasible use the overlap-aware index; without end they fall back t…
  - `core/services/timetable_rooming.py : RoomTracker.intervals : 415 ; _overlapping_rooms : 428-430 ; is_feasible : 443-463 ; assign_best_fit :…` · stages: Greedy first-pass + assign_rooms_to_board filler per-move; repair 1-swap uses overlap query vs DB · flag: always on
  - _Citations confirmed. All in-file call sites pass end=, so overlap-aware is the effective behaviour. Scenario-wide seeded: other-board seeding 813-837, this-board seeding 843-857. A per-move availability filter over a scenario-seeded tracke…_
- **ONLINE placement exclusion (no physical room)** ⚠️CORRECTED — Online placements keep their time slot but must not consume a room: assign_rooms_to_board clears room to '' (bulk_update), skips them in seeding and the assign loop; cross-board seeding skips online rows; validate filters online out before H11-H14 (counted as skipped_online_count).
  - `core/services/timetable_rooming.py : is_online_placement : 775-776 ; online room-clear loop : 789-795 ; other-board seed skip : 821-822 ; t…` · stages: assign_rooms_to_board (per-board sweep); publish-gate validate (scenario-wide) · flag: always on (online-ness resolved per BOARD via OnlineCourseLookup)
  - **correction:** This-board seed skip is lines 844-845 (`if is_online_placement(p): continue`), NOT 845-846 as extracted. Everything else confirmed: online-ness is per-board (is_online_course_for_board / codes_for_board), room cleared even under a lock, legacy stale-room rows…
- **Locked-placement respect (respect_locked)** — When assign_rooms_to_board(respect_locked=True), placements with is_locked are excluded from the reassign set; a locked placement left unroomed is counted into unassigned/locked_skipped rather than being assigned.
  - `core/services/timetable_rooming.py : assign_rooms_to_board (signature default at 733 ; locked_unassigned_count 875-884 ; unassigned_placeme…` · stages: assign_rooms_to_board second-pass filler (per-board) · flag: respect_locked parameter, default False (line 733; callers opt in)
  - _All citations confirmed. Already-roomed locked placements are still seeded as occupancy (loop 843-857 does not exclude locked)._
- **validate_physical_room_compatibility H11-H14 backstop** — For each non-online placement (scenario-wide, or a placement_ids subset), a room code is valid only if >=1 concrete Room with that code SIMULTANEOUSLY satisfies H11 type, H12 capacity>=buffered, H13 no gender OR match, H14 programme intersection. Else violation with per-constraint attribution (or H11-H14_COMBINATION w…
  - `core/services/timetable_rooming.py : validate_physical_room_compatibility : 252-384 (flags 328-335, combination 363-364, not-found 367-376)` · stages: Publish gate (SCENARIO-WIDE by default via board__scenario_id; narrowable via placement_ids) · flag: always on (called by the publish gate; tests/test_timetable_publish_gate.py)
  - _All citations confirmed. Whole-board GATE distinct from per-move filters. H12 checks lab capacity with NO exemption (diverges from greedy room_cap=0). Uses the same room_type_for_placement classifier and buffered demand as placement — a co…_
- **Post-greedy UNASSIGNED repair (direct-fit + 1-swap)** — repair_unassigned_after_greedy: Phase 1 direct assign_best_fit; Phase 2 1-swap — find a room whose current booking overlaps U's window and fits U, relocate its occupant to another free fitting room (same type/gender), give U the vacated room. Occupant demand from budget map else available_capacity else 40. Updates tra…
  - `core/services/timetable_rooming.py : repair_unassigned_after_greedy : 512-678 (direct 560, 1-swap 570-654, overlap occupant DB query 584-59…` · stages: Post-greedy repair pass (per-board) · flag: always on when invoked by the greedy caller (auto_place_board path); no gating flag
  - _All citations confirmed. Uses overlap-aware index + interval DB query (start_time__lt=end, end_time__gt=start) not exact-start. Tracker-restore guards on no-alt (mark_used 648) and alt==same cases confirmed. Enforces type+gender+capacity f…_

### META
- **Best-fit-decreasing allocation policy** ⚠️CORRECTED — Unassigned placements sorted by buffered demand DESC (largest first); room pool sorted capacity ASC so assign_best_fit picks the smallest sufficient room. Packs large sections first; not a hard rejection and not priced into any objective.
  - `core/services/timetable_rooming.py : unassigned_placements.sort(-buffered) : 894-896 ; RoomTracker pool sort capacity ASC : 402-409 ; assig…` · stages: assign_rooms_to_board second-pass filler; simulate_buffer_impact · flag: always on
  - **correction:** Line citations are all accurate, but kind should NOT be SOFT. SOFT per the taxonomy means 'priced in the objective', and this entry's own text states it contributes NO term to the lex objective tuple and is not a rejection. Reclassify as META (a pure ordering…
- **Buffer-only-reject diagnostic (ROOM_BUFFER_REJECT)** ⚠️CORRECTED — When a lecture placement fails at buffered capacity but WOULD fit at raw (tracker.is_feasible with raw_cap, overlap-aware), it is a buffer-only reject: reason ROOM_BUFFER_REJECT and buffer_only_rejects++. Labs excluded (room_type != 'lab' guard). Observability only — the placement is still marked UNASSIGNED.
  - `core/services/timetable_rooming.py : is_buffer_only (inline via tracker.is_feasible) : 997-999 ; refined branch + counter : 1026-1035` · stages: assign_rooms_to_board failure-reason refinement · flag: TIMETABLE_PR2_ROOM_ORACLE_ENABLED default true (config/settings.py:351-352); flag-off -> NO_ROOM_CA…
  - **correction:** rooming.py computes buffer-only INLINE at line 997 (`room_type != 'lab' and tracker.is_feasible(..., raw_cap, ..., end=_e)`); it does NOT call check_buffer_fit. check_buffer_fit exists in timetable_room_oracle.py (defined ~408-452) but is not imported or used…
- **Room-failure reason oracle (type/gender/capacity/occupancy codes)** — For each UNASSIGNED placement, when the oracle flag is on, the reason is refined by first-match chain: buffer-only wins; else check_type_feasibility (NO_ROOM_TYPE) -> check_gender_feasibility (NO_ROOM_GENDER) -> check_capacity_feasibility (NO_ROOM_CAPACITY, buffered) -> check_occupancy (ROOM_OCCUPIED). Default when no…
  - `core/services/timetable_rooming.py : refinement chain : 1024-1058 ; core/services/timetable_room_oracle.py : check_type_feasibility 244-263…` · stages: assign_rooms_to_board failure attribution only (observability) · flag: TIMETABLE_PR2_ROOM_ORACLE_ENABLED default true (config/settings.py:351); flag-off -> None from ever…
  - _All citations confirmed against room_oracle function offsets (type 244, gender 266, capacity 303, occupancy 362; grep-verified). Chain and imports (type/gender/capacity/occupancy all imported and used) confirmed. check_heuristic_match is d…_

## Evaluator/objective (32)

### HARD
- **Student seating order (risk-tier priority)** — Students sorted by (risk_tier, intra_tier_score, student_id) descending, then split: tier>=B seated first with reserves_released=False, tier==C seated after with reserves_released=True. Higher-risk students claim seats first.
  - `core/services/timetable_student_assignment.py:assign_students_to_sections:137-166` · stages: seating (inside evaluate_generated_timetable_candidate) · flag: always on
  - _Sort l.137-141 reverse=True; tier_ab (>=B) l.143/146-155 reserves_released=False; tier_c (==C) l.144/157-166 reserves_released=True. Deterministic. Not a scored term but shapes the board. Confirmed._
- **Per-student course seating order (scarcity + tier rank)** — Unassigned recommended courses sorted by scarcity_key = (tier_rank, feasible_count, -course_rigidity, course_code). tier_rank T1=0/T2=1/T3=2 leads so core seated before foundation/gen-ed; feasible_count = # sections that can_enroll AND don't clash current state.
  - `core/services/timetable_student_assignment.py:assign_courses_for_student:193-206; _tier_rank/COURSE_TIER_RANK:118-125` · stages: seating · flag: tier lead active only when course_tiers threaded (else all rank 0 => original (feasible_count,-rigi…
  - _scarcity_key returned at l.204; feasible_count l.195-197. COURSE_TIER_RANK l.118, _tier_rank l.121-125 (returns 0 with no map). Byte-parity confirmed. Caller MUST thread same tier map. Confirmed._
- **Capacity / reserve enrolment gate (can_enroll + reserve_allowed_for_tier)** — can_enroll(allow_reserve): False if current_enrollment>=max_capacity; True if below regular_limit; else only if allow_reserve. allow_reserve = reserve_allowed_for_tier: True if reserves_released, or tier==A, or (tier==B and allow_tier_b).
  - `core/services/timetable_student_assignment.py:reserve_allowed_for_tier:102-113; timetable_assignment_models.py:can_enroll:84-89,regular_lim…` · stages: seating (every section candidacy check) + repair · flag: always on
  - _can_enroll l.84-89 and regular_limit l.75-76 confirmed. reserve_allowed_for_tier l.107-113 confirmed. Per-move filter, not a whole-board gate. Tier-C reaches reserve seats only in the reserves_released pass. Confirmed._
- **Clash gate in seating (state.has_clash)** — A section is skipped as a candidate if state.has_clash(section.meetings): any meeting whose mask AND occupied_mask_by_day[day] is non-zero. Prevents seating a student into an overlapping section.
  - `core/services/timetable_student_assignment.py:rank_and_select_best_section:231; timetable_assignment_models.py:has_clash:110-114` · stages: seating + repair · flag: always on
  - _l.231 `if state.has_clash(section.meetings): continue`; has_clash l.110-114 confirmed. Per-move filter via per-day bitmask. Distinct from pos1/pos3 clash COUNT. Confirmed._
- **Shallow repair pass (single-blocker relocation with rollback)** — Second pass over students (same risk order). For each unresolved course: try direct placement; else for each enrollable candidate section with EXACTLY ONE blocking assigned course, evict blocker, seat candidate, try to reseat blocker in an alternate section; if alt found commit+mark repaired, else roll back (remove ca…
  - `core/services/timetable_student_assignment.py:repair_unresolved_assignments_shallow:301-401` · stages: seating (post-pass, inside assign_students_to_sections:168-170) · flag: always on; repair course order tier-sorted (stable) only when course_tiers present (321-329)
  - _Risk order l.308-312; direct placement l.335-343; single-blocker filter l.347-354; evict l.377, seat l.381, reseat l.382-391, commit+mark l.390-394, rollback l.395-396. No tier guard on eviction l.356-372. Confirmed._

### OBJECTIVE_POSITION
- **LEGACY pos0 — unresolved_tier_a** — Count of students whose risk_tier == RiskTier.A that have >=1 unresolved course. Incremented once per such student. Most-significant slot of the legacy tuple.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:730-731,798` · stages: canonical evaluator consumed by all V2 stages (generate/rank/local-search/chain/CP-SAT) + rank_timetable_candidates · flag: TIMETABLE_TIERED_OBJECTIVE_ENABLED default true — LEGACY path is the else-branch (flag OFF, or ON w…
  - _Scenario-wide aggregate. Smaller better. Only emitted when tiered path NOT taken (line 707 gate). Increment is inside the `if state.unresolved_courses:` block (727)._
- **LEGACY pos1 — total_unresolved_students** — Count of students with a non-empty state.unresolved_courses dict (any tier).
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:727-728,799` · stages: canonical evaluator (all V2 stages) + ranking · flag: TIERED_OBJECTIVE default true; legacy path only
  - _Scenario-wide. decode_score aliases this as 'blocked_students' (line 689)._
- **LEGACY pos2 — total_unassigned_courses** — Sum over students of len(state.unresolved_courses) — total (student,course) unresolved pairs, tier-blind.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:729,800` · stages: canonical evaluator + ranking · flag: TIERED_OBJECTIVE default true; legacy path only
  - _Scenario-wide count._
- **LEGACY pos3 — total_clashes (assigned double-bookings)** — For each student, gather meetings of all assigned section_ids; count unordered meeting pairs on same day with overlapping bitmask (meetings[i].mask & meetings[j].mask). O(n^2) over that student's meetings.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:749-755,801` · stages: canonical evaluator + ranking · flag: TIERED_OBJECTIVE default true; legacy path only
  - _Scenario-wide sum. Counts ALL assigned meetings via sections_by_id[sec_id].meetings (raw, NOT _campus_meetings) — online sessions ARE counted here. Interval overlap via precomputed per-day bitmask, not exact-start. Confirmed._
- **LEGACY pos4 — gap_minutes (real student gap + same-course spread FOLDED IN)** — total_gap_minutes = real_gap_minutes + spread, where real_gap_minutes = Σ students of state.total_gap_minutes and spread = _compute_same_course_section_spread(...). The legacy tuple BUNDLES the spread pseudo-penalty into the gap slot.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:747,763,796,802` · stages: canonical evaluator + ranking · flag: TIERED_OBJECTIVE default true; legacy path only
  - _Line 796 does total_gap_minutes = real_gap_minutes + spread; line 802 emits it. Online sessions excluded from real gap via _campus_meetings when TIMETABLE_ONLINE_GAP_EXCLUSION_ENABLED (default true). Confirmed._
- **LEGACY pos5 — total_reserve_used** — Σ over all sections of section.reserve_used() = max(0, current_enrollment - regular_limit()), regular_limit = max_capacity - reserve_capacity.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:757-758,803; timetable_assignment_models.py:reserve_used…` · stages: canonical evaluator + ranking · flag: TIERED_OBJECTIVE default true; legacy path only
  - _Scenario-wide. Least-significant of the canonical 6-tuple (unless instr-idle appended). reserve_used 81-82 and regular_limit 75-76 confirmed._
- **LEGACY pos6 — instructor_idle_minutes (optional 7th term)** — Appended ONLY when section_instructor_ids is not None AND is_instructor_gap_penalty_enabled(). Value = _compute_instructor_idle_minutes. Makes the legacy tuple 7-long; strictly lowest priority.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:765-767,808-810` · stages: canonical evaluator + ranking · flag: TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED default false (os.getenv 'false', settings.py:417-419)
  - _When off tuple stays canonical 6-length. is_tiered_score uses length (6/7 legacy vs 9 tiered) with no collision. settings.py:417-419 default 'false' confirmed._
- **TIERED pos0 — hr_unresolved (high-risk unresolved)** — Count of RiskTier.A students that have >=1 unresolved course whose tier is T1 OR T2 (T3-only unresolved does NOT flag the student). HARD, driven to 0.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:732-746,782; TI_HIGHRISK=0 (603)` · stages: canonical evaluator (all V2 stages) + ranking · flag: engaged when course_tiers is not None AND TIMETABLE_TIERED_OBJECTIVE_ENABLED (default true) — line …
  - _Scenario-wide. Line 743 `if is_high and tier in ('T1','T2'): hr_hit=True`. Narrower than legacy pos0. Confirmed._
- **TIERED pos1 — total_clashes** — Identical computation to legacy pos3 (per-student overlapping-mask meeting pairs). HARD; note ~always 0 in practice.
  - `core/services/timetable_student_assignment.py:749-755,783; TI_CLASH=1 (604)` · stages: canonical evaluator + ranking · flag: tiered path (flag default true + map present)
  - _Same online-inclusive clash count as legacy (raw meetings). Confirmed._
- **TIERED pos2 — t1_unresolved** — Count of unresolved (student, course) pairs where course_tiers.get(course,'T1')=='T1'. HARD, drive to 0.
  - `core/services/timetable_student_assignment.py:735-738,784; TI_T1=2 (605)` · stages: canonical evaluator + ranking · flag: tiered path
  - _Line 736 `.get(course, 'T1')` — un-mapped course defaults to 'T1' (hardest). Confirmed._
- **TIERED pos3 — t2_over (Tier-2 unresolved beyond tolerance)** — Per T2 course accumulate unresolved count; t2_over = Σ_course max(0, unresolved_count - tolerance). Near-hard.
  - `core/services/timetable_student_assignment.py:739-740,770-775,785; TI_T2_OVER=3 (606)` · stages: canonical evaluator + ranking · flag: tiered path; tolerance = TIMETABLE_TIERED_T2_TOLERANCE default 3 (floored at 0)
  - _Per-course (t2_unresolved_by_course dict keyed by course code), not scenario-flat. Within-tolerance remainder min(unresolved,tolerance) drops to soft pos5 (line 775). Confirmed._
- **TIERED pos4 — student_cost = real_gap + budget*soft (BOUNDED TRADE)** — student_cost = real_gap_minutes + soft_gap_budget * soft_unresolved. real_gap_minutes = Σ students state.total_gap_minutes. NOT strict priority — gaps and soft enrolment share one axis; a soft seat is taken iff it adds < budget gap-minutes. Real gap recoverable as score[4] - budget*score[5].
  - `core/services/timetable_student_assignment.py:747,776-780,786; TI_STUDENT_COST=4 (611)` · stages: canonical evaluator + ranking · flag: tiered path; budget = TIMETABLE_TIERED_SOFT_GAP_BUDGET default 120 (floored 0; 0 => strict quality-…
  - _Line 780 student_cost = real_gap_minutes + soft_gap_budget * soft_unresolved. Real gap EXCLUDES spread (spread is pos7), unlike legacy pos4. Online exclusion applies to real gap. Confirmed._
- **TIERED pos5 — soft_unresolved (count)** — soft_unresolved = t3_unresolved + t2_within, where t2_within = Σ_course min(unresolved,tolerance). Pure count acting as tie-break and gap-recovery term.
  - `core/services/timetable_student_assignment.py:741,772-776,787; TI_SOFT=5 (612)` · stages: canonical evaluator + ranking · flag: tiered path
  - _Line 776 soft_unresolved = t3_unresolved + t2_within. decode_score recovers real gap = student_cost - budget*soft (669). Confirmed._
- **TIERED pos6 — total_reserve_used** — Σ sections reserve_used(); identical formula to legacy pos5.
  - `core/services/timetable_student_assignment.py:757-758,788; TI_RESERVE=6 (613)` · stages: canonical evaluator + ranking · flag: tiered path
  - _reserve_used_of() accessor reads idx 6 tiered / idx 5 legacy (630-636). Confirmed._
- **TIERED pos7 — same_course_spread (own quality term)** — spread = _compute_same_course_section_spread(sections_by_id); carried as its own slot rather than folded into gap.
  - `core/services/timetable_student_assignment.py:763,789; TI_SPREAD=7 (614)` · stages: canonical evaluator + ranking · flag: tiered path
  - _Unbundling from gap is the point of the tiered layout (real gaps stay visible at pos4). Confirmed._
- **TIERED pos8 — instructor_idle_minutes** — instr_idle = _compute_instructor_idle_minutes when section_instructor_ids not None AND gap flag on, else 0. Lowest-priority slot; always present in the 9-tuple (value 0 when flag off).
  - `core/services/timetable_student_assignment.py:765-767,790; TI_INSTR_IDLE=8 (615)` · stages: canonical evaluator + ranking · flag: TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED default false
  - _Unlike legacy (conditional APPEND) the tiered tuple always has slot 8 (0 when off), fixed length 9. instructor_idle_of() reads idx 8 tiered / trailing idx 6 legacy (639-645). Confirmed._

### SOFT
- **Same-course section-spread penalty (component of pos4 legacy / pos7 tiered)** — Per multi-section course, anchor = earliest meeting per section (min by _meeting_sort_key). Over anchor pairs: different day +1000; overlap(-1) +10000; back-to-back (0<=gap<=15min grace) marks adjacent-pair-found; else +_same_day_gap_penalty (gap<=30 =>30, gap<=120 =>120, else gap). If no adjacent pair for the course,…
  - `core/services/timetable_student_assignment.py:_compute_same_course_section_spread:547-590; timetable_same_course.py:same_course_section_spr…` · stages: computed once in every evaluator path · flag: always on (no flag; unconditional at line 763)
  - _Constants confirmed: DIFFERENT_DAY=1000 (l.9), OVERLAP=10000 (l.10), MISSING_ADJACENT_PAIR=5000 (l.8), BACK_TO_BACK_GRACE=15 (l.7). anchor at l.166; adjacency/penalty logic l.172-185. Uses raw sec.meetings (l.586, NOT _campus_meetings) — o…_
- **Student day-gap metric (_compute_total_state_gap / _compute_day_gap)** — Per student, group campus meetings by day; per day sort by start_min and sum max(0, next.start_min - cur.end_min) over consecutive pairs (0 if <=1 meeting). state.total_gap_minutes is the per-student total, recomputed on every apply/remove_assignment.
  - `core/services/timetable_student_assignment.py:_compute_total_state_gap:456-468,_compute_day_gap:446-453; recompute at apply_assignment:258 …` · stages: maintained during seating; summed into real_gap at evaluator lines 747/716 · flag: always on
  - _Gap uses cur.end_min to next.start_min (edge-to-edge idle), NOT start-to-start (l.452). Online sessions excluded via _campus_meetings (l.464). Line 716 is the real_gap_minutes accumulator init; l.747 the sum. Confirmed._
- **Instructor idle-minutes computation** — Group each instructor's campus meetings (across all sections they teach, per section_instructor_ids) by (instructor,day); sum _compute_day_gap per group. Section taught by N instructors counts toward all N. Sections absent from the map (no instructor) contribute nothing.
  - `core/services/timetable_student_assignment.py:_compute_instructor_idle_minutes:471-502` · stages: evaluator (feeds pos8 tiered / pos6 legacy) · flag: TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED default false
  - _Scenario-wide across instructors. ONLINE excluded via _campus_meetings (l.497). Returns 0 if map empty (l.489-490). N-instructor fan-out at l.496-498. Confirmed._
- **Section-selection per-move objective tuple** — Among sections that pass can_enroll+no-clash, pick min of (0, uses_reserve, added_gap, fill_ratio_bp, section_id): uses_reserve=1 if current_enrollment>=regular_limit; added_gap=calculate_added_gap; fill_ratio_bp=int(((enroll+1)/max(1,cap))*10000). Prefer non-reserve, then least added gap, then fuller section, then lo…
  - `core/services/timetable_student_assignment.py:rank_and_select_best_section:221-243; calculate_added_gap:420-443` · stages: seating (both initial pass and repair direct-placement) · flag: always on
  - _score_tuple built at l.237; uses_reserve l.233, added_gap l.234, fill_ratio_bp l.235-236 (cap=max(1,max_capacity)). added_gap is per-MOVE incremental, online-excluded (l.427), NOT a whole-board gate. Leading 0 is a fixed placeholder slot. …_

### META
- **Online-session gap exclusion (_campus_meetings)** — _campus_meetings(section) returns [] if section.is_online else section.meetings. Applied in calculate_added_gap, _compute_total_state_gap, and _compute_instructor_idle_minutes so online sessions neither create nor bridge a gap chain.
  - `core/services/timetable_student_assignment.py:_campus_meetings:404-417 (used 427,436,464,497)` · stages: student gap + instructor idle computation · flag: TIMETABLE_ONLINE_GAP_EXCLUSION_ENABLED default true (settings.py:519-521)
  - _All four call sites (427,436,464,497) confirmed. is_online set only when flag on, resolved once at SectionState build. Byte-parity: flag off => identity function. Does NOT affect clash counting (pos3/pos1). settings.py:519-521 default 'tru…_
- **instructor_overload_count (side-band diagnostic, NOT in tuple)** — overload_count = count_instructor_daily_overloads = Σ over (instructor,day) of max(0, sessions - cap). Reported on TimetableEvaluationResult.instructor_overload_count; explicitly NEVER part of lexicographic_score.
  - `core/services/timetable_candidate_eval.py:evaluate_generated_timetable_candidate:45-52,62; timetable_pr4_instructor.py:count_instructor_dai…` · stages: candidate evaluation (observability only) · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED default true (settings.py:441-443; is_instructor_daily_cap_e…
  - _candidate_eval computation l.48-52, reported l.62. Confirmed the stale-docstring claim: pr4_instructor.py:101 says 'Default False' but settings.py:442 sets it 'true', so effective runtime default is True. The HARD cap is enforced structura…_
- **Candidate ranking sort key** — rank_timetable_candidates sorts results by (lexicographic_score, int(quality_score['penalty'] or 0), candidate_id). The lexicographic tuple is primary; timetable-quality penalty is a secondary tiebreak; candidate_id final deterministic tiebreak.
  - `core/services/timetable_candidate_eval.py:rank_timetable_candidates:122-129` · stages: ranking (V2 rank stage) · flag: always on
  - _l.122-128 sort key = (r.lexicographic_score, int((r.quality_score or {}).get('penalty') or 0), r.candidate_id). quality_score from evaluate_timetable_quality (separate from objective tuple). Confirmed._
- **Byte-parity contract (tiered flag / online flag / instr-gap flag)** — With course_tiers None OR flag off, evaluate_assignability_lexicographic returns the legacy 6-tuple verbatim (or 7 with instr-gap on); tiered engages only when course_tiers is not None AND flag on (line 707). Seating tier-lead collapses to original order when no map (_tier_rank returns 0). Online exclusion is identity…
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:704-707,793-810; module docstring 52-55; timetable_flags…` · stages: whole objective + seating surface · flag: TIERED_OBJECTIVE default true (settings.py:496-498), ONLINE_GAP_EXCLUSION default true (519-521), I…
  - _All citations confirmed. tiered gate l.707; OFF path l.793-810; docstring byte-parity para l.52-55; is_tiered_objective_enabled l.31-40. In current settings tiered+online ON by default => LIVE objective is the tiered 9-tuple with online-ex…_
- **Tiered engagement guard (map + flag both required)** — tiered = course_tiers is not None AND is_tiered_objective_enabled(). A missing map forces the legacy path even under the flag, so an un-threaded caller stays safe. tolerance/soft_gap_budget read 0 when not tiered.
  - `core/services/timetable_student_assignment.py:evaluate_assignability_lexicographic:704-709; candidate_eval passes course_tiers only if flag…` · stages: evaluator entry + candidate seating · flag: TIERED_OBJECTIVE default true
  - _l.707 tiered = course_tiers is not None and is_tiered_objective_enabled(); l.708-709 tolerance/budget `... if tiered else 0`. candidate_eval l.39 threads course_tiers to seating only if is_tiered_objective_enabled() (score call l.41-42 pas…_
- **T2 tolerance parameter** — get_tiered_t2_tolerance = max(0, int(setting)), default 3. Per-T2-course count of unresolved seats treated as soft (pos5) before the excess becomes near-hard (pos3). Clamped >=0 so a negative misconfig can't let a worse candidate win.
  - `core/services/timetable_flags.py:get_tiered_t2_tolerance:54-62; settings.py:503; used at evaluator 708,770-775` · stages: evaluator (tiered path) · flag: TIMETABLE_TIERED_T2_TOLERANCE default 3
  - _get_tiered_t2_tolerance l.54-62 returns max(0, int(getattr(..., 3))); settings.py:503 default '3'; read at l.708, used l.770-775. Applied per course code. Confirmed._
- **Soft gap budget parameter** — get_tiered_soft_gap_budget = max(0, int(setting)), default 120. Gap-minutes one soft unresolved seat is 'worth'; multiplier in pos4 student_cost. 0 reproduces strict quality-first. Floored >=0.
  - `core/services/timetable_flags.py:get_tiered_soft_gap_budget:65-75; settings.py:509; used 709,780 and decode_score:669` · stages: evaluator (tiered path) + decode · flag: TIMETABLE_TIERED_SOFT_GAP_BUDGET default 120
  - _get_tiered_soft_gap_budget l.65-75 returns max(0, int(getattr(..., 120))); settings.py:509 default '120'; read l.709, used l.780, and reconstructs real gap in decode_score l.669. Confirmed._

## Flags+repairs (23)

### HARD
- **repair_instructor_clashes — interval clash repair pass** — Scenario-wide: detects instructor double-bookings by INTERVAL overlap (greedily keep a non-overlapping set per (instructor,day), preferring locked; overlapping rest are movers). Relocates each mover to a feasible clash-free slot, eval-driven least-student-harm (short-circuits on first slot score <= baseline). Locked o…
  - `core/services/timetable_instructor_cap_repair.py:repair_instructor_clashes:353-602 (detection 426-459; mover relocation 529-583)` · stages: post-build repair pass (persist-time) · flag: gated by TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED (default true); empty report if off (371-373)
  - _Confirmed: fn 353-602; interval predicate 'ks < e and s < ke' at line 444; by_instr_day+detected loop 426-459; mover relocation loop 529-583; cap=10**9 when cap off at 388-389; re-fan+re-room 584-590. All accurate._
- **repair_instructor_clashes — per-move relocation candidate filters** — Relocation target (day2,s2,e2) admitted only if: day2 != current and not an existing meeting-day (all-diff-days); if cap on, no target-day instructor at cap; slot not blocked; slot_count at (day2,s2)==0 (clash-free, exact-start); (day2,s2) not a same-course slot (exact-start); and _creates_board_interval_overlap False…
  - `core/services/timetable_instructor_cap_repair.py:_candidates:504-526 ; _creates_board_interval_overlap:67-93` · stages: clash repair candidate generation (per-move filter) · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED (default true)
  - _Confirmed: _candidates 504-526 (cap check 513, blocked 516, slot_count>0 exact-start 518, course_slots exact-start 520, interval backstop 522-525); _creates_board_interval_overlap 67-93. Mixed exact-start + interval-backstop scheme accurat…_
- **repair_instructor_daily_overloads — relocate-not-drop cap repair pass** — Detects each over-cap (instructor,day) SCENARIO-WIDE and removes excess. For each excess session (latest-start-first) PREFERS relocation over unplacement ALWAYS (relocate vs unplace do NOT compete on student score); among relocations picks least-harm (short-circuits on first slot <= baseline). Unplaces only if NO cap-…
  - `core/services/timetable_instructor_cap_repair.py:repair_instructor_daily_overloads:96-350 (relocate-vs-unplace 248-320; detection 160-165)` · stages: post-build repair pass (persist-time), runs regardless of optimiser improvement · flag: gated by TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (default true); disabled report if off (109-111)
  - _Confirmed: fn 96-350; detection 160-165; per-excess loop 248-323; relocation-preferred comment+logic 256-283. Ghost-guard (placements.remove(p) after pk=None) at 313. Re-fan+re-room 330-336. All accurate._
- **repair_instructor_daily_overloads — per-move relocation candidate filters** — Relocation target feasible only if: day2 != current and not an existing section meeting-day (all-diff-days); NO instructor in set at cap on day2; slot not blocked; (day2,s2) not an existing instructor slot (clash-free, exact-start); (day2,s2) not a same-course slot; and _creates_board_interval_overlap False. Lab-vs-le…
  - `core/services/timetable_instructor_cap_repair.py:_candidate_slots:203-228 ; _creates_board_interval_overlap:67-93` · stages: cap repair candidate generation (per-move filter) · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED (default true)
  - _Confirmed: _candidate_slots 203-228 (dur>75 -> lab_slots at 208, cap check 215, blocked 218, instr_slots exact-start 220, course_slots 222, interval backstop 224-227). Accurate._
- **Instructor compaction — layered student guards (gates_ok)** — Every candidate move must pass all vs pre-pass baseline: (1) lex feasibility slice must not worsen AND reserve_used_of must not rise; (2) if tiered, soft_unresolved must not rise; (3) total student gap <= baseline*(1+gap_budget); (4) Tier-A aggregate gap must not rise; (5) Tier-B (graduating) aggregate gap must not ri…
  - `core/services/timetable_instructor_compaction.py:compact_instructor_schedules.gates_ok:1190-1216` · stages: compaction search (per-move gate) · flag: gated by compaction flag; budgets from get_instructor_compaction_config
  - _gap_ceiling = base_total_gap*(1+gap_budget) at 1188. reserve_used_of/is_tiered_score resolve layout. Empty profiles -> guards vacuous._
- **Instructor compaction — relocation neighbourhood per-move filters** — Candidate target admitted only if: if changing day, day2 not an existing meeting-day AND no instructor in set at cap on day2; H1 duration preserved exactly (e2-s2==dur); not the same (same_day,same start); slot not blocked; NO instructor interval-busy at [s2,e2) on day2; course not interval-busy at [s2,e2). Lab-vs-lec…
  - `core/services/timetable_instructor_compaction.py:_relocation_moves:1123-1153 ; _interval_busy:215-224 ; movable 1218-1223` · stages: compaction search candidate generation (per-move filter) · flag: compaction flag (default false); cap value from get_instructor_daily_cap
  - _Confirmed: _relocation_moves 1123-1153 (cap 1136-1139, H1 dur 1143, same-slot 1145, blocked 1147, instr interval-busy 1149, course interval-busy 1151); _interval_busy 215-224 keyed on (day,start_min,end_min) TRUE interval; movable 1218-122…_
- **Instructor compaction — persisted hard-constraint non-increase gate** — In persist txn, after writing moves + re-rooming, recompute scenario-wide hard counters; if ANY of h15_critical_overlaps, h7_instructor_clashes, h8_daily_overload_sessions, h9_room_clashes, physical_unassigned_rooms increased vs before, raise _UnsafePersistence -> full rollback (DB + in-memory). H15 and H7 kept SEPARA…
  - `core/services/timetable_instructor_compaction.py:_PERSISTENCE_HARD_KEYS:227-233 ; _scenario_persistence_hard_metrics:345-423 ; regression c…` · stages: compaction persistence (whole-scenario gate) · flag: compaction flag (default false)
  - _Confirmed: _PERSISTENCE_HARD_KEYS 227-233; _scenario_persistence_hard_metrics 345-423 (H7/H8 via count_instructor_clashes/count_instructor_daily_overloads 416-421 interval-aware; H15 same-course OR shared_student>=HARD_OVERLAP_THRESHOLD 38…_
- **Instructor compaction — moved-placement room compatibility (H11-H14) gate** — Every moved physical placement validated: room code accepted only if one concrete Room row simultaneously satisfies H11 room_type==required, H12 capacity>=buffered_demand, H13 gender, H14 department intersects board program(s). If greedy re-rooming leaves any moved meeting invalid/unassigned or raises H9/unassigned, a…
  - `core/services/timetable_instructor_compaction.py:_validate_moved_room_compatibility:426-544 ; _exact_repair_affected_rooms:547-892 ; trigge…` · stages: compaction persistence (room repair + verification, same transaction) · flag: compaction flag (default false)
  - _Confirmed: _validate_moved_room_compatibility 426-544 (H11-H14 flags 491-498); _exact_repair_affected_rooms def 547 (time_limit_seconds default 10.0 at 552; num_search_workers=1 820; random_seed=0 821; status!=OPTIMAL->not proven 837-840; …_
- **Instructor compaction — shared multi-board section rejection** — If any term section has placements on >1 board, the whole pass rejects deterministically before evaluating or writing anything: reports unsupported_shared_sections, committed=False, no placements changed.
  - `core/services/timetable_instructor_compaction.py:compact_instructor_schedules:942-1018` · stages: compaction pre-check · flag: compaction flag (default false)
  - _Confirmed: placements_by_section build + shared detection 942-985; if shared_sections report update + early return 986-1018. Accurate._

### OBJECTIVE_POSITION
- **Cap repair least-harm student scoring (lexicographic positions 0-5)** — Candidate relocations/unplacements ranked by full evaluator lexicographic_score (tiered 9-tuple if tiered flag on else legacy). best chosen by sc < best[0]; slot with sc <= baseline taken immediately. student_score_before/after recorded.
  - `core/services/timetable_instructor_cap_repair.py:repair_instructor_daily_overloads._score:175-186 ; selection 266-283` · stages: cap repair (and clash repair _score 475-487) · flag: always on within the repair (gated by the repair's own flag); course_tiers passed only if TIMETABLE…
  - _Confirmed: _score 175-186, selection best/short-circuit 266-283; clash _score 475-487; course_tiers conditional builds at 126 and 471. Cap/clash never enter the lex tuple; student score is tie-break only._
- **Instructor gap penalty — soft objective term** — When on, canonical candidate evaluator appends a lowest-priority instructor_idle_minutes term (strictly below every student + reserve term) and greedy scorer prefers compaction; score tuple grows to 7-tuple. Off -> unchanged 6-tuple (byte-identical).
  - `config/settings.py:module:417-419 ; core/services/timetable_pr4_instructor.py:is_instructor_gap_penalty_enabled:83-93` · stages: candidate evaluator + greedy scorer (computed elsewhere; flag defined on this surface) · flag: TIMETABLE_INSTRUCTOR_GAP_PENALTY_ENABLED default false (helper getattr default False)
  - _Confirmed: settings 417-419 default 'false'; helper 83-93 getattr(...,False). MEMORY notes it proved a NO-OP for real compaction (subordinate term)._

### SOFT
- **Instructor compaction — fair lexicographic instructor objective (search)** — Hill-climb minimises (lower=better): (max_excess_days, total_excess_days, worst_weekly_span, largest_daily_span, total_span, largest_hole, over90, worst_weekly_idle, total_idle, added_student_gap). Max-before-total. Student added_gap is final tie-break. Move accepted only if tuple strictly < current and best in round.
  - `core/services/timetable_instructor_compaction.py:_instructor_compaction_objective:184-203 ; search accept 1281-1314` · stages: compaction search · flag: TIMETABLE_INSTRUCTOR_COMPACTION_ENABLED (default false)
  - _Confirmed: objective tuple 192-203 (largest=largest_hole, worst_weekly=idle, total=idle). Lower bound max(ceil(sessions/cap), max_section_meetings) at _compute_instructor_compaction_metrics 145-148. Online counts as work+days but not campu…_

### META
- **TIMETABLE_INSTRUCTOR_LINKS_ENABLED** — Gates WHERE per-section instructor identity comes from: structured CourseInstructor links (multi-instructor; a meeting clashes if ANY assigned instructor is double-booked) vs the single opaque free-text instructor string. Independent of the clash flag. Sections without links always fall back to free-text.
  - `config/settings.py:module:405-407 ; core/services/timetable_pr4_instructor.py:is_instructor_links_enabled:66-77` · stages: identity source for all instructor-aware stages/repairs · flag: TIMETABLE_INSTRUCTOR_LINKS_ENABLED, default true (os.getenv 'true'); helper getattr fallback False
  - _Confirmed: settings os.getenv(...,'true') 405-407; FLAG_SETTING const at 66, def is_instructor_links_enabled 69, body getattr(...,False) 77. Default ON since 2026-07-16 per comment._
- **Instructor clash — HARD (enforcement flag)** — Gates real-time emission of INSTRUCTOR_CLASH rejection during candidate scoring AND gates the whole repair_instructor_clashes pass (no-ops when off). When off, no instructor-level rejection emitted.
  - `config/settings.py:module:395-397 ; core/services/timetable_pr4_instructor.py:is_instructor_clash_enabled:60-63` · stages: candidate scoring in all optimise stages + repair_instructor_clashes · flag: TIMETABLE_PR4_INSTRUCTOR_CLASH_ENABLED, default true; helper getattr default False
  - _Confirmed: settings 395-397 default 'true'; helper def 60 body 63 getattr(...,False). Clash = different-section time OVERLAP (interval-aware) per has_instructor_clash docstring 167-176._
- **Instructor daily session cap — HARD (enforcement flag + value)** — Gates a HARD constraint: instructor teaches at most TIMETABLE_INSTRUCTOR_DAILY_CAP sessions (lectures AND labs) per day; a 4th forbidden, cap wins vs students. Enforced STRUCTURALLY at candidate generation in every solver stage, NEVER added to lex tuple (flag-off = byte-identical). Also gates repair_instructor_daily_o…
  - `config/settings.py:module:441-444 ; core/services/timetable_pr4_instructor.py:is_instructor_daily_cap_enabled:100-110, get_instructor_daily…` · stages: structural filter in all 5 stages + repair pass · flag: TIMETABLE_INSTRUCTOR_DAILY_CAP_ENABLED default true (helper getattr default False); TIMETABLE_INSTR…
  - _Confirmed: settings flag 441-443 default 'true', value 444 default '3'; helper is_..._enabled 100-110 getattr(...,False), get_..._cap 113-115 getattr(...,3). Default ON since 2026-07-21. Section taught by N instructors counts toward each (…_
- **Instructor compaction — enablement flag (whole pass)** — Gates the post-build compaction pass: first reduces excess teaching days above each instructor's lower bound max(ceil(session_count/daily_cap), H2 distinct-day bound), then shrinks physical-campus span/idle, relocating sessions in TIME only (never who teaches). Runs after cap repair, treats cap as hard gate, room-blin…
  - `config/settings.py:module:459-461 ; core/services/timetable_pr4_instructor.py:is_instructor_compaction_enabled:121-127 ; core/services/time…` · stages: post-build compaction pass · flag: TIMETABLE_INSTRUCTOR_COMPACTION_ENABLED default false (helper getattr default False)
  - _Confirmed: settings 459-461 default 'false'; helper 121-127 getattr(...,False); compact_instructor_schedules early-return 895-901. Short-circuits: cap<=0 925-927, shared multi-board 986-1018, no states 1021-1023, cap_map empty 1060-1062. A…_
- **TIMETABLE_TIERED_OBJECTIVE_ENABLED — tiered lexicographic objective reshape** — When on, candidate evaluator returns a 9-element tiered tuple (high-risk/clash/T1-unresolved/T2-over-tolerance/real-gap/soft-unresolved/reserve/spread/instructor-idle) ranking resolution by course tier. When off, legacy 6-tuple (7 under gap-penalty) shape AND values (byte-identical). Repair/compaction pass course_tier…
  - `config/settings.py:module:496-498 ; core/services/timetable_flags.py:is_tiered_objective_enabled:31-40` · stages: candidate evaluator (used by both repairs + compaction for least-harm scoring) · flag: TIMETABLE_TIERED_OBJECTIVE_ENABLED default true (helper getattr default False)
  - _Confirmed: settings 496-498 default 'true'; helper 31-40 getattr(...,False). Default ON since 2026-07-16. gates_ok/reserve_used_of switch on is_tiered_score. Accurate._
- **TIMETABLE_TIERED_T2_TOLERANCE — per-T2-course soft/near-hard split** — Up to this many unresolved seats in a single Tier-2 course are SOFT (position E / soft_unresolved); excess is NEAR-HARD (position C). Read via get_tiered_t2_tolerance which floors at 0.
  - `config/settings.py:module:503 ; core/services/timetable_flags.py:get_tiered_t2_tolerance:54-62` · stages: tiered evaluator · flag: TIMETABLE_TIERED_T2_TOLERANCE default 3 (max(0,int(getattr default 3)))
  - _Confirmed: settings 503 default '3'; get_tiered_t2_tolerance 54-62 max(0,int(getattr(...,3))). Accurate._
- **TIMETABLE_TIERED_SOFT_GAP_BUDGET — bounded gen-ed-vs-gap trade** — Tiered student-cost position is real_gap_minutes + budget*soft_unresolved; optimiser seats a soft course when it adds fewer than 'budget' gap-minutes. 0 = strict quality-first. Floored at 0 at read time.
  - `config/settings.py:module:509 ; core/services/timetable_flags.py:get_tiered_soft_gap_budget:65-75` · stages: tiered evaluator (student-cost position) · flag: TIMETABLE_TIERED_SOFT_GAP_BUDGET default 120 (max(0,int(getattr default 120)))
  - _Confirmed: settings 509 default '120'; get_tiered_soft_gap_budget 65-75 max(0,int(getattr(...,120))). Accurate._
- **TIMETABLE_ONLINE_GAP_EXCLUSION_ENABLED — exclude ONLINE from gap chains** — When on, ONLINE sessions excluded from student AND instructor gap chains. Read ONCE where SectionStates are built, not in the gap hot loop. When off, no SectionState marked online -> previous online-blind numbers reproduced byte-for-byte.
  - `config/settings.py:module:519-521 ; core/services/timetable_flags.py:is_online_gap_exclusion_enabled:43-51` · stages: SectionState construction / gap evaluation; compaction campus metrics honour section.is_online (_compute_instructor_com… · flag: TIMETABLE_ONLINE_GAP_EXCLUSION_ENABLED default true (helper getattr default False)
  - _Confirmed: settings 519-521 default 'true'; helper 43-51 getattr(...,False). Compaction online-as-work-not-campus at 98-101. Accurate._
- **TIMETABLE_CAPACITY_BUFFER — room-sizing buffer** — Multiplier applied to per-section demand when sizing rooms; 1.1 = 10% buffer. Used to compute buffered_demand that H12 capacity checks enforce in both compaction moved-room validation and exact re-room fallback.
  - `config/settings.py:module:344 ; consumed via timetable_rooming.get_capacity_buffer in compaction _validate_moved_room_compatibility 457 and…` · stages: rooming + compaction room gates · flag: TIMETABLE_CAPACITY_BUFFER default 1.1 (float env)
  - _Confirmed: settings 344 float(os.getenv(...,'1.1')); get_capacity_buffer() call at 457 and 682. Lab capacity enforced too (H12 line 494 applies to all). Accurate._
- **TIMETABLE_ENFORCE_LOCKS — registrar placement locks** — When on, locked SectionPlacement rows preserved through every planner stage and persist path. Both repair passes honour independently: locked never moved/deleted, reported under locked_blocked; compaction excludes locked from movable and its exact re-room treats locked rooms as fixed. When off, locks advisory.
  - `config/settings.py:module:605-610 ; enforced in cap_repair (day_ps 'not p.is_locked' 241, clash detection prefers locked 448-453) and compa…` · stages: all planner stages + both repairs + compaction · flag: TIMETABLE_ENFORCE_LOCKS default true
  - _Confirmed: settings 605-610 default 'true'; cap_repair day_ps filter 'and not p.is_locked' at 241; clash detection locked-first ordering + locked_blocked 448-453; compaction movable 'sid not in locked' 1218-1223; exact-repair locked handli…_
- **Compaction tunables (config bundle)** — get_instructor_compaction_config returns gap_budget (max fractional rise in TOTAL student gap-min), per_student_cap (max added gap-min any single student), trade_ratio (min instructor idle-min saved per student gap-min added), max_rounds (hill-climb rounds), time_budget (wall-clock s; 0/neg disables).
  - `config/settings.py:module:463-481 ; core/services/timetable_pr4_instructor.py:get_instructor_compaction_config:130-142` · stages: compaction search + gates · flag: GAP_BUDGET 0.03, PER_STUDENT_CAP 75, TRADE_RATIO 2.0, MAX_ROUNDS 40, TIME_BUDGET_SECONDS 20 (env-ov…
  - _Confirmed: settings 463-481 (GAP_BUDGET 0.03/464, PER_STUDENT_CAP 75/467, TRADE_RATIO 2.0/470, MAX_ROUNDS 40/473, TIME_BUDGET 20/480); helper 130-142 getattr fallbacks match (0.03, 75, 2.0, 40, 20.0). Accurate._
