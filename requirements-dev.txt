# Development dependencies — includes everything from production
-r requirements.txt

# ── Scraper (requires Playwright browser binary — local dev only) ──
playwright==1.58.0
pyee==13.0.1
greenlet==3.3.2

# ── Testing & Quality ──
ruff==0.12.9
mypy==1.17.1
pytest==8.4.1
pytest-django==4.9.0
coverage==7.10.6
bandit==1.8.6
pip-audit==2.9.0
# setuptools 83.0.0 resolves PYSEC-2026-3447 in the audited CI environment.
setuptools==83.0.0
pre-commit==4.3.0
django-stubs==5.2.2
types-requests==2.32.4.20250809

# ── Policy-store extraction tooling (maintainer scripts, not runtime) ──
# policies/tools/extract_page_text.py reads the source PDF's text layer. Nothing in
# core/ or config/ imports it, so it belongs here and not in requirements.txt — but
# it was undeclared entirely, which is how the Pillow-shaped bug happens: the tool
# runs on the machine that wrote it and nowhere else.
# pypdf 6.16.1 also resolves GHSA-jp53-mhqp-8xcg,
# GHSA-23w6-3w8w-8484, and GHSA-763m-79hh-57f2.
pypdf==6.16.1
