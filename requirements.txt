# ── Core ──
# Django 5.2.15 patch release — resolves pip-audit findings PYSEC-2026-50/
# 54/55 (fixed in 5.2.14) and PYSEC-2026-197/198/199/200/201 (fixed in
# 5.2.15). Routine patch-level bump within the 5.2.x line; no API changes.
Django==5.2.15
# python-dotenv 1.2.2 — resolves CVE-2026-28684. Patch-level bump.
python-dotenv==1.2.2

# ── Database ──
dj-database-url==3.1.2
psycopg2-binary==2.9.11

# ── Static files ──
whitenoise==6.12.0

# ── Production server ──
gunicorn==25.1.0

# ── App dependencies ──
beautifulsoup4==4.14.3
soupsieve==2.8.3
openpyxl==3.1.5
et-xmlfile==2.0.0
tqdm==4.67.3
neo4j==6.2.0

# ── Optimisation (CP-SAT timetable polisher) ──
ortools==9.15.6755

# ── Scraper ──
playwright==1.58.0
