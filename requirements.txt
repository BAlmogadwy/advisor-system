# ── Core ──
# Django 5.2.16 patch release — resolves pip-audit findings PYSEC-2026-2090/
# 2091/2092 (fixed in 5.2.16), on top of the earlier PYSEC-2026-50/54/55 and
# 197-201 fixes. Routine patch-level bump within the 5.2.x line; no API changes.
Django==5.2.16
# python-dotenv 1.2.2 — resolves CVE-2026-28684. Patch-level bump.
python-dotenv==1.2.2

# ── Database ──
dj-database-url==3.1.2
psycopg2-binary==2.9.11

# ── Static files ──
whitenoise==6.12.0

# ── Production server ──
gunicorn==25.1.0

# ── App dependencies ──
beautifulsoup4==4.14.3
# Direct runtime dependency used by the adviser policy store.  Keep this
# explicit so a clean production install does not rely on dev tooling pulling
# it in transitively.
PyYAML==6.0.3
# soupsieve 2.8.4 — resolves pip-audit GHSA-836r-79rf-4m37 + GHSA-2wc2-fm75-p42x
# (fixed in 2.8.4). Patch-level bump; bs4's CSS-select backend, no API change.
soupsieve==2.8.4
openpyxl==3.1.5
et-xmlfile==2.0.0
# Pillow — renders the prerequisite dependency-graph PNG embedded in the
# XLSX export (core.report_views._render_prereq_graph).
Pillow==12.3.0
tqdm==4.67.3
neo4j==6.2.0

# ── Optimisation (CP-SAT timetable polisher) ──
ortools==9.15.6755

# ── Scraper ──
playwright==1.58.0
